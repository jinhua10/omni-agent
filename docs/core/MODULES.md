# OmniAgent 模块架构

> **版本：** 1.0.0  
> **更新时间：** 2026-01-01  
> **状态：** ✅ 生产就绪

---

## 📋 目录

1. [模块总览](#模块总览)
2. [架构分层](#架构分层)
3. [核心模块详解](#核心模块详解)
4. [模块依赖关系](#模块依赖关系)
5. [模块选择指南](#模块选择指南)

---

## 🎯 模块总览

OmniAgent 采用 **Spring Boot Starter 模式**的**完全可插拔架构**，共 **27 个功能模块**，分为 **7 大层次**。

### 模块统计

| 分类 | 模块数 | 说明 |
|------|--------|------|
| **API 接口层** | 9 | 定义核心接口和模型 |
| **Starter 实现层** | 10 | 具体功能实现 |
| **核心基础层** | 2 | core + common |
| **编排协调层** | 1 | orchestrator |
| **Web 服务层** | 2 | web + workflow |
| **算法市场** | 1 | marketplace |
| **示例应用** | 2 | basic + production |
| **总计** | **27** | 全部模块 |

---

## 🏗️ 架构分层

### 完整架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        应用层 (Application Layer)                    │
├─────────────────────────────────────────────────────────────────────┤
│ • omni-agent-example-basic          基础示例应用                     │
│ • omni-agent-example-production     生产环境示例                     │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        Web 服务层 (Web Layer)                        │
├─────────────────────────────────────────────────────────────────────┤
│ • omni-agent-web                    RESTful API 服务                 │
│ • omni-agent-workflow               工作流引擎                       │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                     服务编排层 (Orchestration Layer)                 │
├─────────────────────────────────────────────────────────────────────┤
│ • omni-agent-orchestrator           服务编排协调器                   │
│   - 查询服务编排                                                     │
│   - 上下文管理                                                       │
│   - 只依赖 API 接口                                                  │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    核心智能层 (Intelligence Layer)                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ HOPE 自学习系统 ⭐                                          │   │
│  ├────────────────────────────────────────────────────────────┤   │
│  │ • omni-agent-hope-api          HOPE 接口定义                │   │
│  │ • omni-agent-hope-starter      HOPE 实现（问题分类、学习）  │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ 知识网络系统 ⭐                                             │   │
│  ├────────────────────────────────────────────────────────────┤   │
│  │ • omni-agent-knowledge-registry-api    知识注册表接口       │   │
│  │ • omni-agent-knowledge-registry-starter 知识网络实现        │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    基础服务层 (Service Layer)                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────┐  ┌──────────────────────────────┐    │
│  │ AI 服务                 │  │ 文档处理                      │    │
│  ├─────────────────────────┤  ├──────────────────────────────┤    │
│  │ • omni-agent-ai-api     │  │ • omni-agent-document-       │    │
│  │ • omni-agent-ai-starter │  │   processor-api              │    │
│  │   - Ollama              │  │ • omni-agent-document-       │    │
│  │   - Online API          │  │   processor-starter          │    │
│  │   - ONNX                │  │   - Word/Excel/PPT/PDF       │    │
│  └─────────────────────────┘  │   - Vision LLM               │    │
│                                └──────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────┐  ┌──────────────────────────────┐    │
│  │ RAG 检索                │  │ 智能分块                      │    │
│  ├─────────────────────────┤  ├──────────────────────────────┤    │
│  │ • omni-agent-rag-api    │  │ • omni-agent-chunking-api    │    │
│  │ • omni-agent-rag-       │  │ • omni-agent-chunking-       │    │
│  │   starter-adapter       │  │   starter                    │    │
│  │   - File/Lucene         │  │   - PPL (困惑度) ⭐          │    │
│  │   - Elasticsearch       │  │   - Semantic (语义)          │    │
│  │   - MongoDB             │  │   - Paragraph (段落)         │    │
│  └─────────────────────────┘  └──────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────┐  ┌──────────────────────────────┐    │
│  │ P2P 协作                │  │ OCR 识别                      │    │
│  ├─────────────────────────┤  ├──────────────────────────────┤    │
│  │ • omni-agent-p2p-api    │  │ • omni-agent-ocr-starter-    │    │
│  │ • omni-agent-p2p-       │  │   tesseract                  │    │
│  │   starter               │  │   - Tesseract OCR            │    │
│  └─────────────────────────┘  └──────────────────────────────┘    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      存储层 (Storage Layer)                          │
├─────────────────────────────────────────────────────────────────────┤
│ • omni-agent-document-storage-api        存储接口                    │
│ • omni-agent-document-storage-starter    存储实现                    │
│                                                                      │
│   支持的存储引擎:                                                     │
│   ✅ File (文件系统)                                                 │
│   ✅ SQLite (嵌入式数据库)                                           │
│   ✅ H2 (内存数据库)                                                 │
│   ✅ MongoDB (文档数据库)                                            │
│   ✅ Redis (缓存)                                                    │
│   ✅ Elasticsearch (搜索引擎)                                        │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      核心基础层 (Core Layer)                         │
├─────────────────────────────────────────────────────────────────────┤
│ • omni-agent-core              核心框架和基础设施                     │
│ • omni-agent-common            通用工具类和常量                       │
└─────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    算法市场 (Algorithm Marketplace)                  │
├─────────────────────────────────────────────────────────────────────┤
│ • omni-agent-marketplace       算法组件注册和管理                     │
│   - 查询扩展算法                                                      │
│   - 重排序算法                                                        │
│   - 自定义算法插件                                                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📦 核心模块详解

### 1. 核心基础层

#### omni-agent-common

**职责：** 通用工具类和常量定义

**核心内容：**
- ✅ 通用工具类
- ✅ 常量定义
- ✅ 异常类定义
- ✅ 基础模型

**包路径：** `top.yumbo.ai.omni.common`

#### omni-agent-core

**职责：** 核心框架和基础设施

**核心内容：**
- ✅ 核心基础设施
- ✅ 配置管理
- ✅ 事件系统
- ✅ 不依赖具体实现库

**包路径：** `top.yumbo.ai.omni.core`

**依赖关系：**
```
omni-agent-core
  └─→ omni-agent-common
  └─→ Spring Boot 3.4.1
```

---

### 2. 核心智能层

#### HOPE 自学习系统

**模块组成：**
- **omni-agent-hope-api** - 接口定义
- **omni-agent-hope-starter** - 具体实现

**核心功能：**
```
┌─────────────────────────────────────────────┐
│ HOPE = Hierarchical Omni-Agent             │
│        Persistent Engine                   │
│        (分层智能持久化引擎)                 │
├─────────────────────────────────────────────┤
│                                            │
│ 1. 问题分类器 (QuestionClassifier)         │
│    - 基于关键词和模式分类                   │
│    - 建议使用的知识层级                     │
│                                            │
│ 2. 三层知识管理                             │
│    ├─ Permanent Layer (持久层)            │
│    ├─ Ordinary Layer (普通层)             │
│    └─ High Frequency Layer (高频层)       │
│                                            │
│ 3. 自动学习优化                             │
│    - 访问频率统计                           │
│    - 动态层级调整                           │
│    - 知识自动提升/降级                      │
│                                            │
│ 4. 持久化机制                               │
│    - InMemory 实现 (默认)                 │
│    - KnowledgeRegistry 实现               │
│                                            │
└─────────────────────────────────────────────┘
```

**关键类：**
```java
// API 层
top.yumbo.ai.omni.hope.api.QuestionClassifier
top.yumbo.ai.omni.hope.api.HopePersistence
top.yumbo.ai.omni.hope.api.QuestionTypeConfig

// 实现层
top.yumbo.ai.omni.hope.starter.impl.HOPEKnowledgeManager
top.yumbo.ai.omni.hope.starter.impl.QuestionClassifier
top.yumbo.ai.omni.hope.starter.impl.InMemoryHopePersistence
```

**配置示例：**
```yaml
omni-agent:
  hope:
    enabled: true
    persistence: knowledge-registry  # 或 in-memory
    default-layer: ordinary
```

**详细文档：** [HOPE 系统设计](HOPE_SYSTEM.md)

---

#### 知识网络系统

**模块组成：**
- **omni-agent-knowledge-registry-api** - 接口定义
- **omni-agent-knowledge-registry-starter** - 具体实现

**核心功能：**
```
┌─────────────────────────────────────────────┐
│ Knowledge Network (知识网络)               │
├─────────────────────────────────────────────┤
│                                            │
│ 1. 知识域管理 (Domain Management)          │
│    - 创建和管理知识域                       │
│    - 域类型：Technical/Business/General   │
│    - 独立向量空间                           │
│                                            │
│ 2. 智能检索                                 │
│    ├─ 域路由 (DomainRouter)               │
│    ├─ 跨域查询 (CrossDomainQuery)         │
│    ├─ 质量评分 (QualityScorer)            │
│    └─ 查询缓存 (QueryCache)               │
│                                            │
│ 3. 知识关联                                 │
│    - 自动关联发现                           │
│    - 跨域关联                               │
│    - 引用关系追踪                           │
│                                            │
│ 4. 用户偏好学习                             │
│    - 记录用户行为                           │
│    - 学习域偏好                             │
│    - 个性化检索优化                         │
│                                            │
│ 5. 知识精炼                                 │
│    - AI 提取核心知识                       │
│    - 生成摘要和关键词                       │
│    - 知识质量评估                           │
│                                            │
└─────────────────────────────────────────────┘
```

**关键类：**
```java
// 域管理
top.yumbo.ai.omni.knowledge.registry.network.impl.KnowledgeDomainService
top.yumbo.ai.omni.knowledge.registry.router.DomainRouter

// 知识服务
top.yumbo.ai.omni.knowledge.registry.network.KnowledgeStorageService
top.yumbo.ai.omni.knowledge.registry.network.KnowledgeExtractionService
top.yumbo.ai.omni.knowledge.registry.network.KnowledgeRefinementService
top.yumbo.ai.omni.knowledge.registry.network.KnowledgeAssociationService

// 智能检索
top.yumbo.ai.omni.knowledge.registry.service.query.CrossDomainQueryService
top.yumbo.ai.omni.knowledge.registry.service.quality.DomainQualityScorer
top.yumbo.ai.omni.knowledge.registry.service.cache.QueryResultCache

// 用户偏好
top.yumbo.ai.omni.knowledge.registry.service.preference.UserPreferenceLearner
```

**配置示例：**
```yaml
omni-agent:
  knowledge-registry:
    enabled: true
    cache-size: 1000
    cross-domain-query:
      enabled: true
      thread-pool-size: 10
      timeout: 30000
    quality-scorer:
      enabled: true
      persistence: true
    user-preference:
      enabled: true
      persistence: true
      min-queries: 5
```

**详细文档：** [知识网络架构](KNOWLEDGE_NETWORK.md)

---

### 3. 服务编排层

#### omni-agent-orchestrator

**职责：** 服务编排和协调

**核心功能：**
- ✅ 查询服务编排
- ✅ 上下文管理
- ✅ 工作流协调
- ✅ 只依赖 API 接口，不依赖具体实现

**关键类：**
```java
top.yumbo.ai.omni.orchestrator.QueryOrchestrator
top.yumbo.ai.omni.orchestrator.ContextManager
```

**设计原则：**
- 只依赖各模块的 API 接口
- 不包含具体实现代码
- 负责服务间的协调和编排

---

### 4. 基础服务层

#### AI 服务

**模块组成：**
- **omni-agent-ai-api** - AI 服务接口
- **omni-agent-ai-starter** - AI 服务实现

**支持的 AI 提供商：**

| 提供商 | 类型 | 特点 | 实现类 |
|--------|------|------|--------|
| **Ollama** | 本地 | 免费、隐私、离线 | `OllamaAIService` |
| **DeepSeek** | 在线API | 高性价比 | `OnlineAPIAIService` |
| **OpenAI** | 在线API | 效果好 | `OnlineAPIAIService` |
| **通义千问** | 在线API | 国内快 | `OnlineAPIAIService` |
| **ONNX** | 本地 | 边缘设备 | `OnnxAIService` |

**配置示例：**
```yaml
omni-agent:
  ai:
    provider: ollama              # ollama/deepseek/openai/qwen
    model: qwen2.5:7b
    base-url: http://localhost:11434
    # 或在线 API
    # api-key: ${AI_API_KEY}
```

---

#### 文档处理

**模块组成：**
- **omni-agent-document-processor-api** - 文档处理接口
- **omni-agent-document-processor-starter** - 文档处理实现

**支持的文档格式：**

| 格式 | 处理器 | 功能 |
|------|--------|------|
| **Word** | `WordDocumentProcessor` | .doc/.docx，表格转Markdown |
| **Excel** | `ExcelDocumentProcessor` | .xls/.xlsx，公式计算 |
| **PPT** | `PPTDocumentProcessor` | .ppt/.pptx，幻灯片提取 |
| **PDF** | `PDFDocumentProcessor` | 逐页提取、元数据 |
| **纯文本** | `PlainTextDocumentProcessor` | .txt/.md/.log 等 |
| **图片** | `VisionLLMDocumentProcessor` | AI 图片文字提取 |

**配置示例：**
```yaml
omni-agent:
  document-processor:
    vision-llm:
      enabled: true
      model: qwen-vl-plus
      api-key: ${DASHSCOPE_API_KEY}
```

---

#### 智能分块

**模块组成：**
- **omni-agent-chunking-api** - 分块接口
- **omni-agent-chunking-starter** - 分块实现

**分块策略：**

| 策略 | 实现类 | 说明 | 推荐场景 |
|------|--------|------|----------|
| **PPL** ⭐ | `PPLChunkingStrategy` | 基于困惑度的语义边界 | 通用场景，最智能 |
| **SEMANTIC** | `SemanticStrategy` | 基于向量相似度 | 长文档，语义聚合 |
| **PARAGRAPH** | `ParagraphStrategy` | 基于自然段落 | 格式化文档 |
| **SENTENCE** | `SentenceStrategy` | 基于句子边界 | 短文本、对话 |
| **FIXED_LENGTH** | `FixedLengthStrategy` | 固定长度切分 | 简单场景 |
| **RECURSIVE** | `RecursiveStrategy` | 递归分块 | 大文档 |

**使用示例：**
```java
@Service
public class DocumentService {
    
    @Autowired
    private ChunkingService chunkingService;
    
    public void process(String content) {
        ChunkingConfig config = ChunkingConfig.builder()
            .strategy("PPL")          // 推荐：困惑度分块
            .maxChunkSize(1000)
            .overlapSize(100)
            .build();
            
        List<Chunk> chunks = chunkingService.chunk(content, config);
    }
}
```

---

#### RAG 检索

**模块组成：**
- **omni-agent-rag-api** - RAG 接口
- **omni-agent-rag-starter-adapter** - RAG 适配器

**支持的 RAG 实现：**

| 类型 | 实现类 | 特点 |
|------|--------|------|
| **File/Lucene** | `LuceneRAGService` | 默认，无依赖 |
| **Elasticsearch** | `ElasticsearchRAGService` | 分布式搜索 |
| **MongoDB** | `MongoRAGService` | 文档数据库 |
| **Redis** | `RedisRAGService` | 内存缓存 |

**配置示例：**
```yaml
omni-agent:
  rag:
    instances:
      - id: default
        type: file
        index-path: ./data/rag-index
      - id: es-rag
        type: elasticsearch
        hosts: localhost:9200
```

---

#### P2P 协作

**模块组成：**
- **omni-agent-p2p-api** - P2P 接口
- **omni-agent-p2p-starter** - P2P 实现

**核心功能：**
- ✅ 端到端连接
- ✅ 知识传输
- ✅ 协作学习
- ✅ 连接码机制

**关键类：**
```java
top.yumbo.ai.omni.p2p.api.P2PConnectionManager
top.yumbo.ai.omni.p2p.api.P2PDataTransferService
top.yumbo.ai.omni.p2p.api.P2PCollaborationService
```

**配置示例：**
```yaml
omni-agent:
  p2p:
    enabled: true
    storage-type: memory  # memory/sqlite/h2/redis/mongodb
    connection-code: "unique-code-123"
```

---

#### OCR 识别

**模块：** `omni-agent-ocr-starter-tesseract`

**核心功能：**
- ✅ Tesseract OCR 集成
- ✅ 图片文字识别
- ✅ 多语言支持

**配置示例：**
```yaml
omni-agent:
  ocr:
    tesseract:
      enabled: true
      data-path: ./tessdata
      language: chi_sim  # 中文简体
```

---

### 5. 存储层

#### 文档存储

**模块组成：**
- **omni-agent-document-storage-api** - 存储接口
- **omni-agent-document-storage-starter** - 存储实现

**支持的存储引擎：**

| 引擎 | 实现类 | 适用场景 | 特点 |
|------|--------|---------|------|
| **File** | `FileDocumentStorage` | 开发测试 | 简单、无依赖 |
| **SQLite** | `SQLiteDocumentStorage` | 单机部署 | 嵌入式数据库 |
| **H2** | `H2DocumentStorage` | 内存缓存 | 快速、临时 |
| **MongoDB** | `MongoDocumentStorage` | 生产环境 | 分布式、高性能 |
| **Redis** | `RedisDocumentStorage` | 缓存层 | 超高速 |
| **Elasticsearch** | `ESDocumentStorage` | 大规模搜索 | 全文检索 |

**多实例配置：**
```yaml
omni-agent:
  storage:
    instances:
      - id: primary
        type: file
        base-path: ./data/storage
      - id: backup
        type: mongodb
        database: omni-agent
        collection: documents
```

---

### 6. Web 服务层

#### omni-agent-web

**职责：** RESTful API 服务

**核心功能：**
- ✅ HTTP API 接口
- ✅ 文档上传和管理
- ✅ 查询和检索
- ✅ 用户管理
- ✅ 配置管理

**主要 Controller：**
```java
top.yumbo.ai.omni.web.controller.DocumentController
top.yumbo.ai.omni.web.controller.QueryController
top.yumbo.ai.omni.web.controller.ConfigController
top.yumbo.ai.omni.web.controller.AdvancedQAController
```

---

#### omni-agent-workflow

**职责：** 工作流引擎

**核心功能：**
- ✅ 工作流定义和执行
- ✅ 文件监听和自动索引
- ✅ 文档处理流程
- ✅ 工作流市场

**关键类：**
```java
top.yumbo.ai.omni.workflow.WorkflowRegistry
top.yumbo.ai.omni.workflow.service.FileWatcherService
top.yumbo.ai.omni.workflow.service.DocumentProcessingService
```

**配置示例：**
```yaml
omni-agent:
  workflow:
    storage-type: sqlite
    sqlite-db-path: ./data/workflows/workflows.db
    file-watcher:
      enabled: true
      auto-index: true
      watch-path: ./data/documents
```

---

### 7. 算法市场

#### omni-agent-marketplace

**职责：** 算法组件注册和管理

**核心功能：**
- ✅ 算法组件注册
- ✅ 查询扩展算法
- ✅ 重排序算法
- ✅ 自定义算法插件

**内置算法：**
- `query_expansion` - 查询扩展
- `semantic_chunking` - 语义分块
- `rerank` - 结果重排序

**关键类：**
```java
top.yumbo.ai.omni.marketplace.AlgorithmMarketService
top.yumbo.ai.omni.marketplace.EnhancedQueryService
```

---

### 8. 示例应用

#### omni-agent-example-basic

**职责：** 基础示例应用

**包含功能：**
- ✅ 基础配置示例
- ✅ 本地文件存储
- ✅ Ollama AI 服务
- ✅ HOPE 系统演示

**启动方式：**
```bash
java -jar omni-agent-example-basic-1.0.0.jar
```

---

#### omni-agent-example-production

**职责：** 生产环境示例

**包含功能：**
- ✅ 生产级配置
- ✅ MongoDB 存储
- ✅ Elasticsearch RAG
- ✅ 分布式部署配置

---

## 🔗 模块依赖关系

### 完整依赖图

```
omni-agent-example-basic (示例应用)
  │
  ├─→ omni-agent-web (Web 服务)
  │    ├─→ omni-agent-workflow
  │    ├─→ omni-agent-orchestrator
  │    ├─→ omni-agent-hope-starter
  │    └─→ omni-agent-knowledge-registry-starter
  │
  ├─→ omni-agent-orchestrator (服务编排)
  │    ├─→ omni-agent-hope-api
  │    ├─→ omni-agent-knowledge-registry-api
  │    ├─→ omni-agent-rag-api
  │    └─→ omni-agent-ai-api
  │
  ├─→ omni-agent-hope-starter (HOPE 实现)
  │    ├─→ omni-agent-hope-api
  │    ├─→ omni-agent-knowledge-registry-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-knowledge-registry-starter (知识网络)
  │    ├─→ omni-agent-knowledge-registry-api
  │    ├─→ omni-agent-rag-api
  │    ├─→ omni-agent-ai-api
  │    ├─→ omni-agent-document-storage-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-rag-starter-adapter (RAG 适配器)
  │    ├─→ omni-agent-rag-api
  │    ├─→ omni-agent-ai-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-document-processor-starter (文档处理)
  │    ├─→ omni-agent-document-processor-api
  │    ├─→ omni-agent-chunking-api
  │    ├─→ omni-agent-ai-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-chunking-starter (智能分块)
  │    ├─→ omni-agent-chunking-api
  │    ├─→ omni-agent-ai-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-ai-starter (AI 服务)
  │    ├─→ omni-agent-ai-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-document-storage-starter (文档存储)
  │    ├─→ omni-agent-document-storage-api
  │    └─→ omni-agent-core
  │
  ├─→ omni-agent-p2p-starter (P2P 协作)
  │    ├─→ omni-agent-p2p-api
  │    └─→ omni-agent-core
  │
  └─→ omni-agent-core (核心框架)
       ├─→ omni-agent-common
       └─→ Spring Boot 3.4.1
```

### API 层独立性

所有 API 模块相互独立，不相互依赖：

```
omni-agent-hope-api              (独立)
omni-agent-knowledge-registry-api (独立)
omni-agent-rag-api               (独立)
omni-agent-ai-api                (独立)
omni-agent-chunking-api          (独立)
omni-agent-document-processor-api (独立)
omni-agent-document-storage-api   (独立)
omni-agent-p2p-api               (独立)
```

---

## 🎯 模块选择指南

### 最小化配置（开发测试）

**适用场景：** 本地开发、功能测试

```xml
<dependencies>
    <!-- 核心 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-core</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- AI 服务 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-ai-starter</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- HOPE 系统 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-hope-starter</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- 文档存储 (File) -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-document-storage-starter</artifactId>
        <version>1.0.0</version>
    </dependency>
</dependencies>
```

**配置：**
```yaml
omni-agent:
  ai:
    provider: ollama
    model: qwen2.5:7b
  storage:
    instances:
      - id: default
        type: file
        base-path: ./data/storage
  hope:
    enabled: true
    persistence: in-memory
```

---

### 推荐配置（生产环境）

**适用场景：** 生产部署、完整功能

```xml
<dependencies>
    <!-- Web 服务 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-web</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- HOPE + 知识网络 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-hope-starter</artifactId>
        <version>1.0.0</version>
    </dependency>
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-knowledge-registry-starter</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- 文档处理 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-document-processor-starter</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- RAG -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-rag-starter-adapter</artifactId>
        <version>1.0.0</version>
    </dependency>
    
    <!-- 工作流 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-workflow</artifactId>
        <version>1.0.0</version>
    </dependency>
</dependencies>
```

**配置：**
```yaml
omni-agent:
  ai:
    provider: deepseek
    api-key: ${DEEPSEEK_API_KEY}
  storage:
    instances:
      - id: primary
        type: mongodb
        database: omni-agent
  rag:
    instances:
      - id: default
        type: elasticsearch
        hosts: localhost:9200
  knowledge-registry:
    enabled: true
    cross-domain-query:
      enabled: true
  hope:
    enabled: true
    persistence: knowledge-registry
```

---

### 高级配置（企业级）

**适用场景：** 大规模部署、高可用

**额外模块：**
```xml
<!-- P2P 协作 -->
<dependency>
    <groupId>top.yumbo.ai.omni</groupId>
    <artifactId>omni-agent-p2p-starter</artifactId>
    <version>1.0.0</version>
</dependency>

<!-- 算法市场 -->
<dependency>
    <groupId>top.yumbo.ai.omni</groupId>
    <artifactId>omni-agent-marketplace</artifactId>
    <version>1.0.0</version>
</dependency>
```

**配置特点：**
- ✅ 多存储引擎冗余
- ✅ 分布式 RAG
- ✅ P2P 知识共享
- ✅ 自定义算法插件
- ✅ 完整监控和日志

---

## 📊 模块对比

### API vs Starter

| 特性 | API 模块 | Starter 模块 |
|------|---------|-------------|
| **职责** | 定义接口和模型 | 提供具体实现 |
| **依赖** | 仅依赖 common | 依赖对应 API + core |
| **独立性** | 完全独立 | 依赖 API |
| **扩展性** | 定义扩展点 | 可插拔实现 |
| **示例** | `omni-agent-ai-api` | `omni-agent-ai-starter` |

### 存储引擎对比

| 引擎 | 性能 | 可靠性 | 扩展性 | 适用场景 |
|------|------|-------|-------|---------|
| **File** | ⭐⭐ | ⭐⭐ | ⭐ | 开发测试 |
| **SQLite** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | 单机部署 |
| **H2** | ⭐⭐⭐⭐ | ⭐⭐ | ⭐ | 临时缓存 |
| **MongoDB** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 生产环境 |
| **Redis** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | 缓存层 |
| **Elasticsearch** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 大规模搜索 |

---

## 📚 相关文档

- 🏗️ [完整系统架构](ARCHITECTURE.md) - OmniAgent 整体架构
- 🧠 [HOPE 自学习系统](HOPE_SYSTEM.md) - HOPE 详细设计
- 🕸️ [知识网络架构](KNOWLEDGE_NETWORK.md) - 知识网络详解
- 🚀 [快速开始指南](QUICKSTART.md) - 如何使用各模块

---

**文档维护者：** OmniAgent Team  
**最后更新：** 2026-01-01  
**版本：** 1.0.0


```
┌─────────────────────────────────────────────────────────────┐
│                    应用层 (Application)                      │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-example-basic      示例应用                     │
│ • omni-agent-example-production 生产环境示例                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     Web层 (Web Layer)                        │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-web               RESTful API 服务              │
│ • omni-agent-workflow          工作流引擎                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│              核心智能层 (Intelligence Layer)                 │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-hope-api          HOPE 接口定义                 │
│ • omni-agent-hope-starter      HOPE 实现（问题分类、自学习）  │
│ • omni-agent-orchestrator      编排协调                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│               知识层 (Knowledge Layer)                       │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-knowledge-registry-api      知识注册表接口      │
│ • omni-agent-knowledge-registry-starter  知识网络实现        │
│ • omni-agent-rag-api                     RAG 接口定义        │
│ • omni-agent-rag-starter-adapter         RAG 适配器          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                基础服务层 (Service Layer)                    │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-ai-api                AI 服务接口               │
│ • omni-agent-ai-starter            AI 服务实现               │
│ • omni-agent-chunking-api          分块接口                  │
│ • omni-agent-chunking-starter      分块实现（6种策略）        │
│ • omni-agent-document-processor-api 文档处理接口             │
│ • omni-agent-document-processor-starter 文档处理实现         │
│ • omni-agent-p2p-api               P2P 接口                  │
│ • omni-agent-p2p-starter           P2P 实现                  │
│ • omni-agent-marketplace           算法市场                  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                 存储层 (Storage Layer)                       │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-document-storage-api        存储接口            │
│ • omni-agent-document-storage-starter    存储实现            │
│   支持: File/SQLite/H2/MongoDB/Redis/Elasticsearch          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  核心层 (Core Layer)                         │
├─────────────────────────────────────────────────────────────┤
│ • omni-agent-core    核心框架                                │
│ • omni-agent-common  通用工具类                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 核心模块详解

### 1. HOPE 自学习系统

**模块：** `omni-agent-hope-api` + `omni-agent-hope-starter`

**核心功能：**
- ✅ 问题分类器（QuestionClassifier）
- ✅ 三层知识管理（Permanent/Ordinary/HighFrequency）
- ✅ 智能学习模块（自动优化层级）
- ✅ 统计和监控

**关键类：**
```java
// API 接口
top.yumbo.ai.omni.hope.api.QuestionClassifier
top.yumbo.ai.omni.hope.api.HopePersistence

// 实现类
top.yumbo.ai.omni.hope.starter.impl.HOPEKnowledgeManager
top.yumbo.ai.omni.hope.starter.impl.QuestionClassifier
```

**配置项：**
```yaml
omni-agent:
  hope:
    enabled: true
    persistence: knowledge-registry  # 或 in-memory
```

---

### 2. 知识注册表（Knowledge Registry）

**模块：** `omni-agent-knowledge-registry-api` + `omni-agent-knowledge-registry-starter`

**核心功能：**
- ✅ 域管理（Domain Management）
- ✅ 知识网络构建
- ✅ 知识关联和引用
- ✅ 用户偏好学习
- ✅ 跨域查询

**关键类：**
```java
// 域管理
top.yumbo.ai.omni.knowledge.registry.network.impl.KnowledgeDomainService
top.yumbo.ai.omni.knowledge.registry.router.DomainRouter

// 知识提取
top.yumbo.ai.omni.knowledge.registry.network.impl.DefaultKnowledgeExtractionService

// 用户偏好
top.yumbo.ai.omni.knowledge.registry.statistics.preference.UserPreferenceLearner
```

**配置项：**
```yaml
omni-agent:
  knowledge-registry:
    enabled: true
    cache-size: 1000
    cross-domain-query:
      enabled: true
      thread-pool-size: 10
```

---

### 3. RAG 适配器

**模块：** `omni-agent-rag-api` + `omni-agent-rag-starter-adapter`

**支持的 RAG 实现：**
- ✅ **File/Lucene**（默认）- 本地文件 + Lucene 索引
- ⚠️ **Elasticsearch** - 分布式搜索引擎
- ⚠️ **MongoDB** - 文档数据库
- ⚠️ **Redis** - 内存缓存

**关键类：**
```java
// RAG 服务接口
top.yumbo.ai.omni.rag.api.RAGService

// Lucene 实现
top.yumbo.ai.omni.rag.adapter.impl.file.LuceneRAGService

// RAG 注册表
top.yumbo.ai.omni.rag.adapter.impl.RagServiceRegistry
```

**配置示例：**
```yaml
omni-agent:
  rag:
    instances:
      # 默认 Lucene 实例
      - id: default
        type: file
        index-path: ./data/rag-index/file
        
      # Elasticsearch 实例（可选）
      - id: es-rag
        type: elasticsearch
        hosts: localhost:9200
```

---

### 4. 文档处理器

**模块：** `omni-agent-document-processor-api` + `omni-agent-document-processor-starter`

**支持的文档格式：**
- ✅ **Word** (.doc, .docx)
- ✅ **Excel** (.xls, .xlsx)
- ✅ **PPT** (.ppt, .pptx)
- ✅ **PDF** (.pdf)
- ✅ **纯文本** (.txt, .md, etc.)
- ✅ **Vision LLM** - 图片文字提取

**处理器列表：**
```java
top.yumbo.ai.omni.document.processor.starter.processor.WordDocumentProcessor
top.yumbo.ai.omni.document.processor.starter.processor.ExcelDocumentProcessor
top.yumbo.ai.omni.document.processor.starter.processor.PPTDocumentProcessor
top.yumbo.ai.omni.document.processor.starter.processor.PDFDocumentProcessor
top.yumbo.ai.omni.document.processor.starter.processor.PlainTextDocumentProcessor
top.yumbo.ai.omni.document.processor.starter.processor.VisionLLMDocumentProcessor
```

**配置示例：**
```yaml
omni-agent:
  document-processor:
    vision-llm:
      enabled: true
      model: qwen-vl-plus
      api-key: ${DASHSCOPE_API_KEY}
```

---

### 5. 智能分块（Chunking）

**模块：** `omni-agent-chunking-api` + `omni-agent-chunking-starter`

**分块策略：**

| 策略 | 说明 | 推荐场景 |
|------|------|----------|
| **PPL** ⭐ | 基于困惑度的语义边界 | 通用场景，最智能 |
| **SEMANTIC** | 基于向量相似度 | 长文档，需要语义聚合 |
| **PARAGRAPH** | 基于自然段落 | 格式化文档 |
| **SENTENCE** | 基于句子边界 | 短文本、对话 |
| **FIXED_LENGTH** | 固定长度切分 | 简单场景 |
| **CUSTOM** | 自定义策略 | 特殊需求 |

**关键类：**
```java
// 策略接口
top.yumbo.ai.omni.chunking.api.ChunkingStrategy

// PPL 策略（推荐）
top.yumbo.ai.omni.chunking.starter.strategy.PPLChunkingStrategy

// 语义策略
top.yumbo.ai.omni.chunking.starter.strategy.SemanticStrategy
```

**使用示例：**
```java
@Service
public class DocumentService {
    
    private final ChunkingService chunkingService;
    
    public void processDocument(String content) {
        // 使用 PPL 策略
        ChunkingConfig config = ChunkingConfig.builder()
            .strategy("PPL")
            .maxChunkSize(1000)
            .overlapSize(100)
            .build();
            
        List<Chunk> chunks = chunkingService.chunk(content, config);
    }
}
```

---

### 6. AI 服务

**模块：** `omni-agent-ai-api` + `omni-agent-ai-starter`

**支持的 AI 提供商：**

| 提供商 | 类型 | 特点 |
|--------|------|------|
| **Ollama** | 本地推理 | 免费、隐私、离线可用 |
| **DeepSeek** | 在线API | 高性价比、效果好 |
| **OpenAI** | 在线API | 效果最好、成本高 |
| **通义千问** | 在线API | 国内访问快 |
| **ONNX** | 本地推理 | 边缘设备、嵌入式 |

**关键类：**
```java
// AI 服务接口
top.yumbo.ai.omni.ai.api.AIService

// Ollama 实现
top.yumbo.ai.omni.ai.starter.impl.OllamaAIService

// 在线 API 实现
top.yumbo.ai.omni.ai.starter.impl.OnlineAPIAIService

// ONNX 实现
top.yumbo.ai.omni.ai.starter.impl.OnnxAIService
```

**配置示例：**
```yaml
omni-agent:
  ai:
    # Ollama 配置
    provider: ollama
    model: qwen2.5:7b
    base-url: http://localhost:11434
    
    # 或者在线 API
    # provider: deepseek
    # model: deepseek-chat
    # api-key: ${DEEPSEEK_API_KEY}
```

---

### 7. 文档存储

**模块：** `omni-agent-document-storage-api` + `omni-agent-document-storage-starter`

**支持的存储引擎：**

| 存储类型 | 适用场景 | 特点 |
|---------|---------|------|
| **File** | 开发测试 | 简单、无依赖 |
| **SQLite** | 单机部署 | 嵌入式数据库 |
| **H2** | 内存缓存 | 快速、临时存储 |
| **MongoDB** | 生产环境 | 分布式、高性能 |
| **Redis** | 缓存层 | 超高速、内存存储 |
| **Elasticsearch** | 大规模搜索 | 全文检索、分布式 |

**关键类：**
```java
// 存储服务接口
top.yumbo.ai.omni.storage.api.DocumentStorageService

// File 实现
top.yumbo.ai.omni.storage.impl.file.FileDocumentStorage

// MongoDB 实现
top.yumbo.ai.omni.storage.impl.mongodb.MongoDocumentStorage
```

**多实例配置：**
```yaml
omni-agent:
  storage:
    instances:
      # 主存储（File）
      - id: primary
        type: file
        base-path: ./data/storage
        
      # 备份存储（MongoDB）
      - id: backup
        type: mongodb
        database: omni-agent
        collection: documents
```

---

### 8. P2P 知识共享

**模块：** `omni-agent-p2p-api` + `omni-agent-p2p-starter`

**核心功能：**
- ✅ 端到端连接
- ✅ 知识传输
- ✅ 协作学习
- ✅ 连接码机制

**存储支持：**
- Memory（内存）
- SQLite（单机）
- H2（临时）
- Redis（分布式）
- MongoDB（持久化）

**关键类：**
```java
// P2P 连接管理
top.yumbo.ai.omni.p2p.api.P2PConnectionManager

// 数据传输
top.yumbo.ai.omni.p2p.api.P2PDataTransferService

// 协作服务
top.yumbo.ai.omni.p2p.api.P2PCollaborationService
```

**配置示例：**
```yaml
omni-agent:
  p2p:
    enabled: true
    storage-type: sqlite
    connection-code: "my-unique-code-123"
    auto-sync: true
```

---

### 9. 工作流引擎

**模块：** `omni-agent-workflow`

**核心功能：**
- ✅ 工作流定义和执行
- ✅ 文件监听和自动索引
- ✅ 文档处理流程
- ✅ 工作流市场

**关键类：**
```java
// 工作流注册表
top.yumbo.ai.omni.workflow.WorkflowRegistry

// 文件监听服务
top.yumbo.ai.omni.workflow.service.FileWatcherService

// 文档处理服务
top.yumbo.ai.omni.workflow.service.DocumentProcessingService
```

**配置示例：**
```yaml
omni-agent:
  workflow:
    storage-type: sqlite
    sqlite-db-path: ./data/workflows/workflows.db
    file-watcher:
      enabled: true
      auto-index: true
      watch-path: ./data/documents
```

---

### 10. 算法市场

**模块：** `omni-agent-marketplace`

**核心功能：**
- ✅ 算法组件注册
- ✅ 查询扩展
- ✅ 重排序算法
- ✅ 自定义算法

**内置算法：**
- **query_expansion** - 查询扩展
- **semantic_chunking** - 语义分块
- **rerank** - 重排序

**关键类：**
```java
// 算法市场服务
top.yumbo.ai.omni.marketplace.AlgorithmMarketService

// 查询增强服务
top.yumbo.ai.omni.marketplace.EnhancedQueryService
```

---

## 🔧 模块依赖关系

```
omni-agent-example-basic
  ├─→ omni-agent-web
  │    ├─→ omni-agent-workflow
  │    ├─→ omni-agent-hope-starter
  │    └─→ omni-agent-knowledge-registry-starter
  │
  ├─→ omni-agent-hope-starter
  │    ├─→ omni-agent-hope-api
  │    └─→ omni-agent-knowledge-registry-api
  │
  ├─→ omni-agent-knowledge-registry-starter
  │    ├─→ omni-agent-knowledge-registry-api
  │    ├─→ omni-agent-rag-api
  │    └─→ omni-agent-storage-api
  │
  ├─→ omni-agent-rag-starter-adapter
  │    ├─→ omni-agent-rag-api
  │    └─→ omni-agent-ai-api
  │
  ├─→ omni-agent-document-processor-starter
  │    ├─→ omni-agent-document-processor-api
  │    ├─→ omni-agent-chunking-api
  │    └─→ omni-agent-ai-api
  │
  └─→ omni-agent-core
       ├─→ omni-agent-common
       └─→ Spring Boot 3.4.1
```

---

## 📊 模块统计

| 分类 | 模块数 | 说明 |
|------|--------|------|
| **API 接口** | 8 | 定义核心接口 |
| **Starter 实现** | 12 | 具体实现 |
| **核心模块** | 2 | core + common |
| **Web 模块** | 2 | web + workflow |
| **示例应用** | 2 | basic + production |
| **总计** | **26** | 全部模块 |

---

## 🎯 模块选择指南

### 最小化配置（开发测试）

```xml
<dependencies>
    <!-- 核心 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-core</artifactId>
    </dependency>
    
    <!-- AI 服务 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-ai-starter</artifactId>
    </dependency>
    
    <!-- HOPE 系统 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-hope-starter</artifactId>
    </dependency>
</dependencies>
```

### 推荐配置（生产环境）

```xml
<dependencies>
    <!-- Web 服务 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-web</artifactId>
    </dependency>
    
    <!-- HOPE + 知识网络 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-hope-starter</artifactId>
    </dependency>
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-knowledge-registry-starter</artifactId>
    </dependency>
    
    <!-- 文档处理 -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-document-processor-starter</artifactId>
    </dependency>
    
    <!-- RAG -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-rag-starter-adapter</artifactId>
    </dependency>
</dependencies>
```

---

## 📚 相关文档

- 📖 [完整架构](ARCHITECTURE.md)
- 🧠 [HOPE 系统](HOPE_SYSTEM.md)
- 🕸️ [知识网络](KNOWLEDGE_NETWORK.md)
- 🚀 [快速开始](QUICKSTART.md)

---

**文档维护者：** OmniAgent Team  
**最后更新：** 2026-01-01

