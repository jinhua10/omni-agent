# RAG 2.0 代码规范与文档管理规范
# RAG 2.0 Code Standards and Document Management

> **文档编号**: 20251209-23-00-00-CODE_STANDARDS  
> **创建日期**: 2025-12-09 23:00:00  
> **类型**: 代码规范  
> **状态**: ✅ 生效中  
> **优先级**: 🔥 必须遵守

---

## ⚠️ 重要：每个新 Session 必读

**在任何新的 Copilot Session 中开始编码前，请告诉 Copilot**：
```
"遵守 20251209-23-00-00-CODE_STANDARDS.md 中的所有代码规范"
```

或者：
```
"按照 RAG 2.0 代码规范开发"
```

**Copilot 会自动**：
1. ✅ 读取本文档
2. ✅ 应用所有代码规范
3. ✅ 生成符合规范的代码

---

## 📂 文档管理规范

### 1. 目录结构

```
docs/refactor/
├── 20251209-23-00-00-CODE_STANDARDS.md          (本文档 - 代码规范)
├── 20251209-22-29-00-IMPLEMENTATION_PLAN.md     (主实施计划 - 持续更新)
├── SESSION_RECOVERY_GUIDE.md                     (会话恢复指南)
├── README.md                                     (目录说明)
│
├── phase-1/
│   └── 20251210-xx-xx-Phase1-Progress.md         (Phase 1 进度 - 持续更新)
│
├── phase-2/
│   └── 20251211-xx-xx-Phase2-Progress.md         (Phase 2 进度 - 持续更新)
│
├── ...
│
└── done/                                         (已完成的阶段性报告)
    ├── 20251215-xx-xx-Phase1-Complete.md         (Phase 1 完成报告)
    ├── 20251220-xx-xx-Phase2-Complete.md         (Phase 2 完成报告)
    └── ...
```

### 2. 文档更新策略

#### 规则 A: 进度始终在同一个文档更新
```yaml
❌ 错误做法:
  - Phase1-Progress-Day1.md
  - Phase1-Progress-Day2.md
  - Phase1-Progress-Day3.md
  # 进度分散，难以追踪

✅ 正确做法:
  - phase-1/20251210-10-00-00-Phase1-Progress.md
    ├─ 2025-12-10 10:00 - 开始 Phase 1
    ├─ 2025-12-10 14:30 - 完成任务 1.1
    ├─ 2025-12-11 09:00 - 完成任务 1.2
    └─ 2025-12-12 16:00 - Phase 1 完成
  # 所有进度在一个文档中，清晰可追溯
```

#### 规则 B: 阶段完成时生成报告
```yaml
触发条件:
  - Phase X 的所有任务都完成
  - 验收标准全部通过
  
生成报告:
  文件: done/yyyyMMdd-HH-mm-ss-PhaseX-Complete.md
  内容:
    - 完成的功能列表
    - 代码统计
    - 测试结果
    - 遇到的问题和解决方案
    - 经验教训
    - 下一步建议
```

#### 规则 C: 文档命名规范
```yaml
持续更新的文档:
  格式: phase-{N}/yyyyMMdd-HH-mm-ss-Phase{N}-Progress.md
  示例: phase-1/20251210-10-00-00-Phase1-Progress.md
  
完成报告:
  格式: done/yyyyMMdd-HH-mm-ss-Phase{N}-Complete.md
  示例: done/20251215-16-30-00-Phase1-Complete.md
  
其他文档:
  格式: yyyyMMdd-HH-mm-ss-{描述}.md
  示例: 20251209-23-00-00-CODE_STANDARDS.md
```

---

## 💻 代码规范

### 1. 注释格式规范

#### 规则 1.1: 类注释
```java
/**
 * 角色检测器 (Role Detector)
 * 
 * 负责分析用户问题，识别所属角色
 * (Responsible for analyzing user questions and identifying roles)
 * 
 * @author AI Reviewer Team
 * @since 2.0.0
 */
public class RoleDetector {
    // ...
}
```

#### 规则 1.2: 方法注释
```java
/**
 * 检测问题所属的角色 (Detect roles for the question)
 * 
 * @param question 用户问题 (User question)
 * @param context 上下文信息 (Context information)
 * @return 角色评分列表 (List of role scores)
 */
public List<RoleScore> detect(String question, String context) {
    // 实现逻辑...
}
```

#### 规则 1.3: 行内注释
```java
// 1. 提取关键词 (Extract keywords)
List<String> keywords = extractKeywords(question);

// 2. 计算相似度 (Calculate similarity)
double similarity = calculateSimilarity(keywords, roleKeywords);

// 3. 返回评分 (Return score)
return new RoleScore(roleId, similarity);
```

#### 规则 1.4: 复杂逻辑注释
```java
/**
 * 融合多个角色的检索结果
 * (Fuse search results from multiple roles)
 * 
 * 算法说明 (Algorithm description):
 * 1. 收集所有角色的检索结果
 * 2. 对相同文档的分数进行加权累加
 * 3. 按最终分数排序
 * 4. 返回 Top K 结果
 */
private List<Document> fuseResults(List<RoleSearchResult> results, int topK) {
    // ...
}
```

---

### 2. Lombok 注解规范

#### ⭐ 核心原则：优先使用 @Data
```
默认规则：所有 POJO/DTO/VO/实体类都使用 @Data 注解
例外情况：只有在特殊业务逻辑或安全需求时才手动编写 getter/setter
```

#### 规则 2.1: 使用 @Data 简化 POJO
```java
// ❌ 错误做法 - 手动编写 getter/setter
public class UserInfo {
    private String userId;
    private String userName;
    private int age;
    
    public String getUserId() {
        return userId;
    }
    
    public void setUserId(String userId) {
        this.userId = userId;
    }
    
    public String getUserName() {
        return userName;
    }
    
    public void setUserName(String userName) {
        this.userName = userName;
    }
    
    // ... 更多 getter/setter
}

// ✅ 正确做法 - 使用 @Data 注解
import lombok.Data;

@Data
public class UserInfo {
    private String userId;
    private String userName;
    private int age;
}
```

#### 规则 2.2: 使用 @Getter/@Setter 精确控制
```java
// 场景：只需要 Getter，不需要 Setter
import lombok.Getter;

@Getter
public class ImmutableConfig {
    private final String configKey;
    private final String configValue;
    
    public ImmutableConfig(String key, String value) {
        this.configKey = key;
        this.configValue = value;
    }
}

// 场景：部分字段需要自定义 Setter 逻辑
import lombok.Getter;
import lombok.Setter;

@Getter
public class UserProfile {
    @Setter
    private String nickname;
    
    private String email; // 只读
    
    // 自定义 Setter 逻辑 (Custom setter logic)
    public void setEmail(String email) {
        if (email != null && email.contains("@")) {
            this.email = email.toLowerCase();
        }
    }
}
```

#### 规则 2.3: 使用 @Builder 构建复杂对象
```java
import lombok.Builder;
import lombok.Data;

@Data
@Builder
public class SearchRequest {
    private String query;
    private String userId;
    private List<String> roles;
    private int topK;
    private double threshold;
}

// 使用示例 (Usage p2p)
SearchRequest request = SearchRequest.builder()
    .query("如何使用Spring Boot")
    .userId("user123")
    .roles(Arrays.asList("developer", "architect"))
    .topK(10)
    .threshold(0.7)
    .build();
```

#### 规则 2.4: 内部类使用 Lombok
```java
// ✅ 推荐：聚合结果内部类使用 @Data
import lombok.Data;

public class SignalAggregator {
    
    @Data
    public static class UserAggregation {
        private String userId;
        private AttitudeScore averageAttitude;
        private Map<SignalType, Long> signalTypeDistribution;
        private long positiveSignalCount;
        private long negativeSignalCount;
        private String tendency;
        private String attitudeTrend;
        
        public UserAggregation(String userId) {
            this.userId = userId;
        }
    }
    
    @Data
    public static class ConceptAggregation {
        private String answerId;
        private double averageScore;
        private long positiveFeedbackCount;
        private long negativeFeedbackCount;
        private double feedbackRatio;
        private boolean problematic;
        private double confidenceIndex;
        
        public ConceptAggregation(String answerId) {
            this.answerId = answerId;
        }
    }
}
```

#### 规则 2.5: Lombok 注解选择指南
```yaml
默认规则 - 优先使用 @Data:
  ✅ 适用于 99% 的场景:
    - 所有 DTO/VO/POJO
    - 内部数据类
    - 聚合结果类
    - 配置类
    - 请求/响应对象
    - 实体类（Entity）
    - 数据模型类
  
  使用方式:
    - 简单类：直接使用 @Data
    - 需要构造函数：@Data + 自定义构造函数
    - 不可变对象：@Data + final 字段

特殊场景 - 手动编写 getter/setter:
  ⚠️ 仅在以下情况手动编写:
    
    1. 安全需求（字段脱敏）:
       @Getter
       private String password;
       
       // 手动编写 setter 进行加密
       public void setPassword(String password) {
           this.password = encrypt(password);
       }
    
    2. 数据验证:
       @Getter
       private String email;
       
       // 手动编写 setter 进行验证
       public void setEmail(String email) {
           if (!email.contains("@")) {
               throw new IllegalArgumentException("Invalid email");
           }
           this.email = email.toLowerCase();
       }
    
    3. 数据格式化:
       @Getter
       private BigDecimal price;
       
       // 手动编写 getter 进行格式化
       public String getFormattedPrice() {
           return String.format("$%.2f", price);
       }
    
    4. 延迟加载:
       private List<Item> items;
       
       public List<Item> getItems() {
           if (items == null) {
               items = loadItems();
           }
           return items;
       }

其他注解配合使用:
  @Builder:
    - 参数较多的对象（>3个）
    - 可与 @Data 同时使用
  
  @AllArgsConstructor / @NoArgsConstructor:
    - JPA 实体类要求
    - 可与 @Data 同时使用
  
  @ToString / @EqualsAndHashCode:
    - @Data 已包含，一般不需要单独使用
    - 除非需要自定义排除字段
```

#### 规则 2.6: Lombok 与其他注解配合
```java
// ✅ 与 JPA 配合
import lombok.Data;
import javax.persistence.*;

@Data
@Entity
@Table(name = "users")
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(nullable = false)
    private String username;
    
    @Column(nullable = false)
    private String email;
}

// ✅ 与 Jackson 配合
import lombok.Data;
import com.fasterxml.jackson.annotation.JsonProperty;

@Data
public class ApiResponse {
    @JsonProperty("status_code")
    private int statusCode;
    
    @JsonProperty("error_message")
    private String errorMessage;
    
    private Object data;
}
```

---

### 3. 日志规范

#### 规则 3.1: 日志级别
```java
// DEBUG - 详细的调试信息 (Detailed debugging information)
log.debug("角色检测: 关键词={}, 匹配数={}", keywords, matchCount);

// INFO - 重要的业务流程 (Important business process)
log.info("开始角色检测: question={}", question);

// WARN - 警告信息，但不影响正常流程 (Warning, but not affect normal process)
log.warn("角色检测置信度较低: score={}", score);

// ERROR - 错误信息，需要关注 (Error, need attention)
log.error("角色检测失败: question={}, error={}", question, e.getMessage(), e);
```

#### 规则 3.2: 日志国际化（强制要求）

**🔥 核心原则：所有日志消息必须使用国际化**

```java
// ❌ 错误做法 - 日志硬编码（禁止）
log.info("开始处理用户请求");
log.error("角色检测失败: {}", errorMsg);
log.debug("查询参数: keyword={}", keyword);

// ✅ 正确做法 - 使用 I18N 国际化
log.info(I18N.get("role.detector.start"), question);
log.error(I18N.get("role.detector.failed"), errorMsg);
log.debug(I18N.get("role.query.params"), keyword);
```

**日志级别使用规范**:
```java
// DEBUG - 详细调试信息（开发调试用）
log.debug(I18N.get("role.filter.applying"), keyword);

// INFO - 重要业务流程（生产环境关键节点）
log.info(I18N.get("role.list.loaded"), roleCount);

// WARN - 警告但不影响主流程（需要关注）
log.warn(I18N.get("role.config.missing"), configPath);

// ERROR - 错误影响功能（需要处理）
log.error(I18N.get("role.create.failed"), e.getMessage(), e);
```

**日志 Emoji 使用规范**:
```yaml
推荐使用的 Emoji 前缀:
  🚀 - 启动/开始操作
  ✅ - 成功完成
  ❌ - 失败/错误
  ⚠️  - 警告
  🔍 - 搜索/查询
  📝 - 记录/保存
  🗑️  - 删除
  🔄 - 刷新/重新加载
  ⚙️  - 配置/设置
  📊 - 统计/分析
  🤖 - AI相关操作
```

#### 规则 3.3: 国际化文件管理（强制分离）

**🔥 核心原则：中英文必须分别存储在不同的配置文件和目录**

```yaml
目录结构（强制规范）:
  src/main/resources/i18n/
    ├── zh/                          # 中文目录
    │   ├── zh_messages.yml          # 老代码兼容
    │   ├── zh-common.yml            # 通用消息
    │   ├── zh-role.yml              # 角色模块
    │   ├── zh-document.yml          # 文档模块
    │   ├── zh-feedback.yml          # 反馈模块
    │   ├── zh-vector-index.yml      # 向量索引模块
    │   └── zh-error.yml             # 错误消息
    │
    └── en/                          # 英文目录
        ├── en_messages.yml          # 老代码兼容
        ├── en-common.yml            # 通用消息
        ├── en-role.yml              # 角色模块
        ├── en-document.yml          # 文档模块
        ├── en-feedback.yml          # 反馈模块
        ├── en-vector-index.yml      # 向量索引模块
        └── en-error.yml             # 错误消息

文件命名规范:
  ✅ 正确: zh-role.yml, en-role.yml
  ❌ 错误: role-zh.yml, role_en.yml
  
  规则:
    1. 文件名格式: {语言}-{模块}.yml
    2. 语言代码: zh（中文）、en（英文）
    3. 模块名称: 小写字母，多个单词用短横线连接
```

#### 规则 3.4: 国际化键值规范

**键名命名规范**:
```yaml
格式: {模块}.{子模块}.{操作}.{详情}

示例:
  # 角色模块
  role.list.loaded: "✅ 角色列表加载成功: total={0}, returned={1}"
  role.create.success: "✅ 创建角色成功: {0}"
  role.delete.failed: "❌ 删除角色失败: {0}"
  
  # 文档模块  
  document.search.start: "🔍 开始搜索文档: keyword={0}"
  document.upload.success: "✅ 文档上传成功: {0}"
  
  # 错误消息
  error.validation.required: "❌ 必填字段不能为空: {0}"
  error.database.connection: "❌ 数据库连接失败: {0}"
```

#### 规则 3.5: 国际化检查清单（开发规范）

**新增国际化键值时（必须遵守）**:
```yaml
步骤:
  1. 确定模块和键名:
     - 使用语义化命名
     - 遵循 module.submodule.action.detail 格式
  
  2. 同时在中英文文件中添加:
     ✅ zh/zh-role.yml
     ✅ en/en-role.yml
  
  3. 确保内容对应:
     - 键名完全一致
     - 参数占位符数量和顺序一致
     - 使用 {0}, {1}, {2} 等占位符
  
  4. 添加注释:
     - 说明使用场景
     - 注明参数含义

示例:
  # zh/zh-role.yml
  role:
    query:
      # 参数: {0}=keyword, {1}=page, {2}=pageSize
      start: "🔍 查询角色: keyword={0}, page={1}, pageSize={2}"
  
  # en/en-role.yml  
  role:
    query:
      # Parameters: {0}=keyword, {1}=page, {2}=pageSize
      start: "🔍 Querying roles: keyword={0}, page={1}, pageSize={2}"
```

**更新国际化文件时（必须检查）**:
```yaml
检查项:
  ✅ 两个语言文件都已更新
  ✅ 键名拼写完全一致
  ✅ 参数占位符数量和顺序一致
  ✅ 没有遗漏的模块文件
  ✅ 参数类型匹配（字符串、数字等）
  ✅ 特殊字符正确转义
```

**代码中使用国际化（最佳实践）**:
```java
// ✅ 正确 - 直接使用 I18N.get
log.info(I18N.get("role.create.success"), roleName);

// ✅ 正确 - 多个参数
log.debug(I18N.get("role.query.result"), keyword, total, returned);

// ❌ 错误 - 不要拼接国际化结果
String msg = I18N.get("role.prefix") + roleName; // 禁止

// ❌ 错误 - 不要在国际化键中硬编码值
log.info(I18N.get("role.status." + status)); // 禁止

// ✅ 正确 - 将状态作为参数传递
log.info(I18N.get("role.status.changed"), status);
```

---

### 4. 常量字符串国际化

#### 规则 4.1: 常量定义
```java
// ❌ 错误做法 - 硬编码字符串
public class RoleDetector {
    private static final String ERROR_MSG = "角色检测失败";
    
    public void detect() {
        throw new RuntimeException(ERROR_MSG);
    }
}

// ✅ 正确做法 - 使用国际化键
public class RoleDetector {
    public void detect() {
        throw new RuntimeException(I18n.get("role.detector.error"));
    }
}
```

#### 规则 4.2: YAML 文件结构
```yaml
文件位置: src/main/resources/i18n/

目录结构（按语言分目录）:
  src/main/resources/i18n/
    ├── zh/                          (中文目录)
    │   ├── zh_messages.yml          (老代码兼容 - messages_zh.yml)
    │   ├── zh-common.yml            (通用消息)
    │   ├── zh-role-detector.yml     (角色检测模块)
    │   ├── zh-vector-index.yml      (向量索引模块)
    │   ├── zh-concept-evolution.yml (概念演化模块)
    │   ├── zh-feedback.yml          (反馈模块)
    │   └── zh-error.yml             (错误消息)
    │
    └── en/                          (英文目录)
        ├── en_messages.yml          (老代码兼容 - messages_en.yml)
        ├── en-common.yml            (通用消息)
        ├── en-role-detector.yml     (角色检测模块)
        ├── en-vector-index.yml      (向量索引模块)
        ├── en-concept-evolution.yml (概念演化模块)
        ├── en-feedback.yml          (反馈模块)
        └── en-error.yml             (错误消息)

命名规范:
  - 文件按语言分目录存放，便于管理
  - 文件名保留语言前缀（zh-/en-），便于识别
  - 特殊文件：zh_messages.yml 和 en_messages.yml 用于兼容老代码
```

#### 规则 3.3: YAML 内容格式

**中文文件示例** (`zh/zh-role-detector.yml`):
```yaml
# 中文角色检测消息
# 文件: src/main/resources/i18n/zh/zh-role-detector.yml

role:
  detector:
    start: "🚀 开始角色检测: question={0}"
    success: "✅ 角色检测成功: role={0}, confidence={1}"
    failed: "❌ 角色检测失败: {0}"
    keyword-match: "🔍 关键词匹配: keywords={0}, matchCount={1}"
    ai-analysis: "🤖 AI 智能分析中..."
```

**英文文件示例** (`en/en-role-detector.yml`):
```yaml
# English Role Detector Messages
# File: src/main/resources/i18n/en/en-role-detector.yml

role:
  detector:
    start: "🚀 Starting role detection: question={0}"
    success: "✅ Role detection succeeded: role={0}, confidence={1}"
    failed: "❌ Role detection failed: {0}"
    keyword-match: "🔍 Keyword matching: keywords={0}, matchCount={1}"
    ai-analysis: "🤖 AI analyzing..."
```

**关键特性**:
- ✅ 中英文完全分开，便于管理
- ✅ 文件名用 `zh-` 和 `en-` 前缀区分
- ✅ 使用 `{0}`, `{1}` 作为参数占位符（MessageFormat 格式）
- ✅ 支持 Emoji，提升日志可读性

---

### 4. 代码组织规范

#### 规则 4.1: 包结构
```
top.yumbo.ai.omni
├── role/                      (角色相关)
│   ├── Role.java
│   ├── RoleConfig.java
│   ├── RoleDetector.java
│   └── RoleManager.java
│
├── concept/                   (概念相关)
│   ├── ConceptUnit.java
│   ├── ConceptVersion.java
│   ├── ConceptUpdater.java
│   └── VersionManager.java
│
├── index/                     (索引相关)
│   ├── RoleVectorIndex.java
│   ├── DocumentClassifier.java
│   └── IndexBuilder.java
│
├── retriever/                 (检索相关)
│   ├── MultiRoleRetriever.java
│   ├── ResultFusion.java
│   └── WeightCalculator.java
│
└── i18n/                      (国际化)
    ├── I18n.java
    └── MessageLoader.java
```

#### 规则 4.2: 类命名
```java
// 服务类 (Service classes)
RoleDetectorService
ConceptEvolutionService

// 管理类 (Manager classes)
RoleManager
VersionManager

// 仓储类 (Repository classes)
ConceptRepository
RoleRepository

// 工具类 (Utility classes)
RoleUtils
ConceptUtils

// 配置类 (Configuration classes)
RoleConfig
IndexConfig
```

---

### 5. 国际化工具类

#### 规则 5.1: I18n 工具类实现

**已实现**: `src/main/java/top/yumbo/ai/omni/i18n/I18N.java`

**核心特性**:
- ✅ 静态自动加载：应用启动时自动加载 `i18n/zh/` 和 `i18n/en/` 目录下的所有 YAML 文件
- ✅ 目录分离：中文文件放在 `zh/` 目录，英文文件放在 `en/` 目录
- ✅ 中英文分离：分别加载到 `messagesZh` 和 `messagesEn` 两个 Map
- ✅ 老代码兼容：自动加载 `messages` 模块（对应 `zh_messages.yml` 和 `en_messages.yml`）
- ✅ 智能语言检测：根据系统属性/环境变量/默认语言自动选择
- ✅ 参数格式化：使用 `MessageFormat` 支持 `{0}`, `{1}` 占位符
- ✅ 降级处理：找不到 key 时返回 `[key]` 而不是抛异常

**加载逻辑**:
```java
static {
    // 加载中文消息 (Load Chinese messages)
    // 扫描 i18n/zh/ 目录下的所有文件 (Scan all files in i18n/zh/)
    loadMessagesWithPrefix("i18n/zh/zh-", messagesZh, "Chinese");
    
    // 加载英文消息 (Load English messages)
    // 扫描 i18n/en/ 目录下的所有文件 (Scan all files in i18n/en/)
    loadMessagesWithPrefix("i18n/en/en-", messagesEn, "English");
}
```

**使用方法**:
```java
// 自动根据系统语言选择中英文 (Auto select language)
log.info(I18N.get("role.detector.start", question));

// 指定语言（用于 API 响应） (Specify language for API)
String msg = I18N.getLang("error.file.not-found", "en", filename);

// Controller 返回前端消息（强制使用 getLang）
@GetMapping("/api/data")
public ResponseEntity<?> getData(@RequestHeader(value = "Accept-Language", defaultValue = "zh") String lang) {
    // ✅ 正确 - 使用 getLang 支持前端语言切换
    return ResponseEntity.ok(Map.of(
        "message", I18N.getLang("data.load.success", lang, count),
        "data", data
    ));
}

// ❌ 错误 - 不要使用 get() 返回前端，会导致前端切换语言失效
return ResponseEntity.ok(Map.of(
    "message", I18N.get("data.load.success", count), // 禁止！只能用于日志
    "data", data
));
```

**⚠️ 重要：参数传递的正确方式**

```java
// ✅ 正确：参数传入 I18N.get() 方法内部
log.info(I18N.get("role.query.start", keyword, page, pageSize));
log.debug(I18N.get("feedback.conflicts.query.start", status, page, pageSize));

// ❌ 错误：参数传给 log 方法（会导致占位符不被替换）
log.info(I18N.get("role.query.start"), keyword, page, pageSize);
log.debug(I18N.get("feedback.conflicts.query.start"), status, page, pageSize);
// 结果：日志显示 "🔍 查询角色: keyword={0}, page={1}, pageSize={2}"（占位符未替换）

// ✅ 正确：多行参数也要在 I18N.get() 内部
log.debug(I18N.get("feedback.conflicts.query.start", 
        status, 
        page, 
        pageSize));

// ❌ 错误：多行参数放在外面
log.debug(I18N.get("feedback.conflicts.query.start"), 
        status, 
        page, 
        pageSize);
```

**为什么会出错？**

- `I18N.get()` 使用 `MessageFormat.format()` 进行参数替换，占位符格式是 `{0}`, `{1}`, `{2}`
- SLF4J 的 `log.info()` 使用不同的占位符格式 `{}`，与 MessageFormat 不兼容
- 如果参数传给 `log.info()`，`I18N.get()` 无法获取参数，返回未格式化的字符串
- SLF4J 看到 `{0}` 不认识，原样输出

**检测方法：** 如果日志中看到 `{0}`, `{1}` 这种占位符，说明参数传递错误。

**预定义模块**:
系统会自动加载以下模块的国际化文件：
- `messages` - 老代码兼容（zh_messages.yml / en_messages.yml）
- `common` - 通用消息
- `role-detector` - 角色检测
- `vector-index` - 向量索引
- `concept-evolution` - 概念演化
- `feedback` - 反馈系统
- `retriever` - 检索器
- `streaming` - 流式响应
- `error` - 错误消息

---

#### 规则 5.2: 前端消息国际化（强制要求）

**核心原则**: 所有返回给前端的消息必须使用 `I18N.getLang(String key, String lang, Object... args)` 方法，支持前端语言切换。

**方法签名**:
```java
/**
 * 获取指定语言的国际化消息
 * @param key  消息键
 * @param lang 语言代码 (zh/en/null)
 * @param args 格式化参数
 * @return 国际化消息
 */
public static String getLang(String key, String lang, Object... args)
```

**强制规则**:

1. **Controller 层返回消息必须使用 getLang**:
```java
// ✅ 正确 - 从 Header 获取语言参数
@GetMapping("/api/roles")
public ResponseEntity<?> getRoles(
    @RequestHeader(value = "Accept-Language", defaultValue = "zh") String lang) {
    
    List<Role> roles = roleService.getAllRoles();
    return ResponseEntity.ok(Map.of(
        "message", I18N.getLang("role.list.loaded", lang, roles.size()),
        "data", roles
    ));
}

// ✅ 正确 - 异常处理也要使用 getLang
@ExceptionHandler(ResourceNotFoundException.class)
public ResponseEntity<?> handleNotFound(
    ResourceNotFoundException e,
    @RequestHeader(value = "Accept-Language", defaultValue = "zh") String lang) {
    
    return ResponseEntity.status(404).body(Map.of(
        "error", I18N.getLang("error.resource.notfound", lang, e.getResourceId())
    ));
}

// ❌ 错误 - 使用 get() 会导致前端切换语言失效
return ResponseEntity.ok(Map.of(
    "message", I18N.get("role.list.loaded", roles.size()) // 禁止！
));
```

2. **ResponseEntity 统一包装**:
```java
// ✅ 推荐 - 创建统一响应工具类
public class ApiResponse {
    public static <T> ResponseEntity<Map<String, Object>> success(
        String messageKey, String lang, T data, Object... args) {
        
        return ResponseEntity.ok(Map.of(
            "success", true,
            "message", I18N.getLang(messageKey, lang, args),
            "data", data
        ));
    }
    
    public static ResponseEntity<Map<String, Object>> error(
        String messageKey, String lang, int status, Object... args) {
        
        return ResponseEntity.status(status).body(Map.of(
            "success", false,
            "error", I18N.getLang(messageKey, lang, args)
        ));
    }
}

// 使用统一响应
@GetMapping("/api/roles/{id}")
public ResponseEntity<?> getRole(
    @PathVariable String id,
    @RequestHeader(value = "Accept-Language", defaultValue = "zh") String lang) {
    
    Role role = roleService.getRole(id);
    if (role == null) {
        return ApiResponse.error("role.notfound", lang, 404, id);
    }
    return ApiResponse.success("role.get.success", lang, role);
}
```

3. **Service 层保持使用 get()（仅日志）**:
```java
// ✅ 正确 - Service 层日志使用 get()
public class RoleService {
    public List<Role> getAllRoles() {
        log.info(I18N.get("role.query.start")); // 日志用 get()
        List<Role> roles = roleRepository.findAll();
        log.info(I18N.get("role.query.success"), roles.size());
        return roles;
    }
}

// ❌ 错误 - Service 层不要返回国际化消息给 Controller
public String createRole(Role role) {
    roleRepository.save(role);
    return I18N.get("role.create.success"); // 禁止！让 Controller 处理
}
```

4. **前端语言参数传递**:
```javascript
// ✅ 前端发送语言 Header
const lang = localStorage.getItem('language') || 'zh';

axios.get('/api/roles', {
    headers: {
        'Accept-Language': lang
    }
}).then(response => {
    console.log(response.data.message); // 自动切换中英文
});

// 切换语言时重新请求
function switchLanguage(newLang) {
    localStorage.setItem('language', newLang);
    // 重新加载数据，消息会自动更新
    loadRoles();
}
```

**检查清单**:
- [ ] Controller 所有返回消息使用 `getLang(key, lang, args)`
- [ ] 从 `Accept-Language` Header 获取语言参数
- [ ] Service 层日志使用 `get(key, args)`（不需要 lang）
- [ ] 异常处理器支持 lang 参数
- [ ] 前端发送 `Accept-Language` Header
- [ ] 前端切换语言后刷新数据

**常见错误对比**:
```java
// ❌ 错误 1: Controller 使用 get() 而非 getLang()
@GetMapping("/api/data")
public ResponseEntity<?> getData() {
    return ResponseEntity.ok(Map.of(
        "message", I18N.get("data.loaded") // 前端切换语言不生效
    ));
}

// ✅ 正确 1: 使用 getLang() 支持前端语言切换
@GetMapping("/api/data")
public ResponseEntity<?> getData(
    @RequestHeader(value = "Accept-Language", defaultValue = "zh") String lang) {
    return ResponseEntity.ok(Map.of(
        "message", I18N.getLang("data.loaded", lang)
    ));
}

// ❌ 错误 2: Service 层返回国际化消息
public String processData() {
    // ... 处理逻辑
    return I18N.get("data.processed"); // Service 不应该返回消息
}

// ✅ 正确 2: Service 层只记录日志，消息由 Controller 生成
public void processData() {
    log.info(I18N.get("data.processing.start"));
    // ... 处理逻辑
    log.info(I18N.get("data.processing.success"));
    // Controller 层负责生成返回消息
}

// ❌ 错误 3: 忘记添加 lang 参数
@PostMapping("/api/roles")
public ResponseEntity<?> createRole(@RequestBody Role role) {
    roleService.save(role);
    return ResponseEntity.ok(Map.of(
        "message", I18N.getLang("role.create.success", null, role.getName())
        // lang 是 null，会降级到系统默认语言
    ));
}

// ✅ 正确 3: 始终传递 lang 参数
@PostMapping("/api/roles")
public ResponseEntity<?> createRole(
    @RequestBody Role role,
    @RequestHeader(value = "Accept-Language", defaultValue = "zh") String lang) {
    roleService.save(role);
    return ResponseEntity.ok(Map.of(
        "message", I18N.getLang("role.create.success", lang, role.getName())
    ));
}
```

---

### 6. 使用示例

#### 示例 6.1: 创建 YAML 文件

**中文文件** (`zh/zh-role-detector.yml`):
```yaml
# 中文角色检测消息
# 文件: src/main/resources/i18n/zh/zh-role-detector.yml

role:
  detector:
    start: "🚀 开始角色检测: question={0}"
    success: "✅ 角色检测成功: role={0}, confidence={1}"
    failed: "❌ 角色检测失败: {0}"
    keyword-match: "🔍 关键词匹配: keywords={0}, count={1}"
    ai-analysis: "🤖 AI 智能分析中..."
    low-confidence: "⚠️ 置信度较低: score={0}"
```

**英文文件** (`en/en-role-detector.yml`):
```yaml
# English Role Detector Messages
# File: src/main/resources/i18n/en/en-role-detector.yml

role:
  detector:
    start: "🚀 Starting role detection: question={0}"
    success: "✅ Role detection succeeded: role={0}, confidence={1}"
    failed: "❌ Role detection failed: {0}"
    keyword-match: "🔍 Keyword matching: keywords={0}, count={1}"
    ai-analysis: "🤖 AI analyzing..."
    low-confidence: "⚠️ Low confidence: score={0}"
```

#### 示例 6.2: 在代码中使用
```java
package top.yumbo.ai.omni.role;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import top.yumbo.ai.omni.i18n.I18N;

import java.util.List;

/**
 * 角色检测器 (Role Detector)
 * 
 * 负责分析用户问题，识别所属角色
 * (Responsible for analyzing user questions and identifying roles)
 * 
 * @author AI Reviewer Team
 * @since 2.0.0
 */
@Slf4j
@Service
public class RoleDetector {
    
    /**
     * 检测角色 (Detect role)
     * 
     * @param question 用户问题 (User question)
     * @return 角色评分 (Role score)
     */
    public RoleScore detect(String question) {
        // 开始检测 (Start detection)
        log.info(I18N.get("role.detector.start", question));
        
        try {
            // 1. 关键词匹配 (Keyword matching)
            List<String> keywords = extractKeywords(question);
            int matchCount = countMatches(keywords);
            log.debug(I18N.get("role.detector.keyword-match", keywords, matchCount));
            
            // 2. 计算评分 (Calculate score)
            double score = (double) matchCount / keywords.size();
            
            // 3. 检查置信度 (Check confidence)
            if (score < 0.5) {
                log.warn(I18N.get("role.detector.low-confidence", score));
            }
            
            // 4. 返回结果 (Return result)
            RoleScore result = new RoleScore("developer", score);
            log.info(I18N.get("role.detector.success", result.getRoleId(), score));
            
            return result;
            
        } catch (Exception e) {
            log.error(I18N.get("role.detector.failed", e.getMessage()), e);
            throw new RuntimeException(I18N.get("role.detector.failed", e.getMessage()), e);
        }
    }
    
    // ...其他方法...
}
```

**关键点**:
- ✅ 无需手动加载文件，`I18N` 类在静态初始化时已自动加载所有 `zh-*.yml` 和 `en-*.yml` 文件
- ✅ 使用 `I18N.get(key, args...)` 获取消息，系统自动根据语言环境选择中英文
- ✅ 参数使用 `{0}`, `{1}` 占位符，自动格式化

---

## 🎨 前端代码规范

### 核心原则：以 JSX 为主实现前端组件

**⭐ 重要规则 ⭐**: 所有新的前端代码必须使用 **JSX 格式**实现，避免使用纯 JavaScript 创建 DOM 元素。

#### 为什么选择 JSX？

1. **可读性更强**: JSX 的标签语法更接近 HTML，易于理解和维护
2. **类型安全**: 配合 React，JSX 提供更好的类型检查
3. **组件化**: JSX 天然支持组件化开发，便于复用
4. **生态完善**: React 生态系统成熟，工具链完善
5. **团队协作**: 统一的代码风格，降低沟通成本

---

### 规则 6: JSX 优先规范

#### 规则 6.1: 必须使用 JSX 实现新组件

**❌ 错误做法** - 使用纯 JavaScript 创建 DOM:
```javascript
// 不推荐：使用纯 JavaScript 创建元素
function createButton() {
    const button = document.createElement('button');
    button.className = 'btn btn-primary';
    button.textContent = 'Click Me';
    button.onclick = handleClick;
    return button;
}

// 不推荐：使用字符串拼接 HTML
function createCard() {
    return `
        <div class="card">
            <h3>${title}</h3>
            <p>${content}</p>
        </div>
    `;
}
```

**✅ 正确做法** - 使用 JSX:
```jsx
// 推荐：使用 JSX 创建组件
function Button({ onClick, children }) {
    return (
        <button className="btn btn-primary" onClick={onClick}>
            {children}
        </button>
    );
}

// 推荐：JSX 组件清晰易读
function Card({ title, content }) {
    return (
        <div className="card">
            <h3>{title}</h3>
            <p>{content}</p>
        </div>
    );
}
```

#### 规则 6.2: JSX 文件命名和组织

**文件命名规范**:
```yaml
组件文件:
  格式: ComponentName.jsx
  位置: src/main/resources/static/js/components/
  
示例:
  ✅ WelcomeGuide.jsx          (引导页面组件)
  ✅ DualTrackAnswer.jsx       (双轨答案组件)
  ✅ HOPEDashboardPanel.jsx    (HOPE 仪表盘组件)
  ❌ welcome-guide.js          (错误：不是 JSX，命名不规范)
  ❌ welcomeGuide.jsx          (错误：应使用 PascalCase)

目录结构:
  src/main/resources/static/js/components/
    ├── common/                (通用组件)
    │   ├── Button.jsx
    │   ├── Modal.jsx
    │   └── LanguageContext.js (Context 可以是 .js)
    ├── tabs/                  (Tab 组件)
    │   ├── QATab.jsx
    │   ├── DocumentsTab.jsx
    │   └── StatisticsTab.jsx
    ├── WelcomeGuide.jsx       (顶层组件)
    ├── DualTrackAnswer.jsx
    └── App.jsx                (主应用)
```

#### 规则 6.3: JSX 组件结构规范

**标准 JSX 组件结构**:
```jsx
/**
 * 组件名称 (Component Name)
 * 功能描述 (Function description)
 * 
 * @author AI Reviewer Team
 * @since 2025-12-10
 */

// 1. 导入 React 和 Hooks (Import React and Hooks)
const { useState, useEffect, useCallback } = React;

// 2. 定义组件 (Define component)
function MyComponent({ prop1, prop2, onAction }) {
    // 3. 状态定义 (State definition)
    const [state1, setState1] = useState(initialValue);
    const [state2, setState2] = useState(null);
    
    // 4. 国际化 (Internationalization)
    const { t } = window.LanguageModule.useTranslation();
    
    // 5. 副作用 (Side effects)
    useEffect(() => {
        // 初始化逻辑 (Initialization logic)
        loadData();
    }, []);
    
    // 6. 事件处理函数 (Event handlers)
    const handleClick = useCallback(() => {
        // 处理点击 (Handle click)
        setState1(newValue);
        onAction?.();
    }, [onAction]);
    
    // 7. 辅助函数 (Helper functions)
    const formatData = (data) => {
        // 格式化数据 (Format data)
        return data.toString();
    };
    
    // 8. 渲染 (Render)
    return (
        <div className="my-component">
            {/* 组件内容 (Component content) */}
            <h2>{t('myTitle')}</h2>
            <button onClick={handleClick}>
                {t('myButton')}
            </button>
        </div>
    );
}

// 9. 导出到全局 (Export to global)
window.MyComponent = MyComponent;
```

#### 规则 6.4: JSX 代码风格

**缩进和格式**:
```jsx
// ✅ 正确：清晰的缩进和换行
function MyComponent() {
    return (
        <div className="container">
            <header>
                <h1>Title</h1>
            </header>
            <main>
                <p>Content</p>
            </main>
        </div>
    );
}

// ❌ 错误：所有代码挤在一行
function MyComponent() {
    return <div className="container"><header><h1>Title</h1></header><main><p>Content</p></main></div>;
}
```

**条件渲染**:
```jsx
// ✅ 推荐：使用 && 操作符
{isLoading && <LoadingSpinner />}
{error && <ErrorMessage message={error} />}

// ✅ 推荐：使用三元运算符
{isLoggedIn ? <Dashboard /> : <LoginForm />}

// ✅ 推荐：复杂条件提取为函数
const renderContent = () => {
    if (isLoading) return <LoadingSpinner />;
    if (error) return <ErrorMessage message={error} />;
    if (data) return <DataView data={data} />;
    return <EmptyState />;
};

return (
    <div className="container">
        {renderContent()}
    </div>
);
```

**列表渲染**:
```jsx
// ✅ 正确：使用 map 和 key
<ul>
    {items.map(item => (
        <li key={item.id}>
            {item.name}
        </li>
    ))}
</ul>

// ❌ 错误：没有 key 属性
<ul>
    {items.map(item => (
        <li>{item.name}</li>
    ))}
</ul>

// ❌ 错误：使用 index 作为 key（如果列表会变化）
<ul>
    {items.map((item, index) => (
        <li key={index}>{item.name}</li>
    ))}
</ul>
```

#### 规则 6.5: Props 和 State 管理

**Props 解构**:
```jsx
// ✅ 推荐：在参数中解构 props
function MyComponent({ title, content, onSave, isEditing = false }) {
    return (
        <div>
            <h2>{title}</h2>
            <p>{content}</p>
            {isEditing && <button onClick={onSave}>Save</button>}
        </div>
    );
}

// ❌ 不推荐：在组件内部解构
function MyComponent(props) {
    const { title, content, onSave, isEditing } = props;
    // ...
}
```

**State 命名**:
```jsx
// ✅ 推荐：清晰的命名
const [isLoading, setIsLoading] = useState(false);
const [documents, setDocuments] = useState([]);
const [selectedDocument, setSelectedDocument] = useState(null);
const [showModal, setShowModal] = useState(false);

// ❌ 不推荐：不清晰的命名
const [flag, setFlag] = useState(false);
const [data, setData] = useState([]);
const [item, setItem] = useState(null);
```

#### 规则 6.6: 事件处理

**事件处理命名**:
```jsx
// ✅ 推荐：使用 handle 前缀
const handleClick = () => { /* ... */ };
const handleSubmit = () => { /* ... */ };
const handleChange = (e) => { /* ... */ };
const handleDelete = (id) => { /* ... */ };

// ✅ 推荐：传递给子组件用 on 前缀
<ChildComponent
    onClick={handleClick}
    onSubmit={handleSubmit}
    onDelete={handleDelete}
/>
```

**避免内联函数**:
```jsx
// ❌ 不推荐：内联函数（每次渲染都创建新函数）
<button onClick={() => handleDelete(item.id)}>Delete</button>

// ✅ 推荐：使用 useCallback
const handleDeleteClick = useCallback(() => {
    handleDelete(item.id);
}, [item.id]);

<button onClick={handleDeleteClick}>Delete</button>

// ✅ 推荐：简单场景可以接受内联（如切换布尔值）
<button onClick={() => setShowModal(true)}>Open</button>
```

#### 规则 6.7: 注释和文档

**组件文档注释**:
```jsx
/**
 * 文档上传组件 (Document Upload Component)
 * 
 * 功能：支持拖拽上传、批量上传、文件类型限制
 * (Features: Drag & drop, batch upload, file type restriction)
 * 
 * @param {string} uploadUrl - 上传 API 地址 (Upload API URL)
 * @param {Function} onSuccess - 上传成功回调 (Success callback)
 * @param {Function} onError - 上传失败回调 (Error callback)
 * @param {Array<string>} acceptedTypes - 允许的文件类型 (Accepted file types)
 * @param {number} maxSize - 最大文件大小（MB）(Max file size in MB)
 * 
 * @example
 * <DocumentUpload
 *     uploadUrl="/api/documents/upload"
 *     onSuccess={handleUploadSuccess}
 *     onError={handleUploadError}
 *     acceptedTypes={['.pdf', '.docx']}
 *     maxSize={100}
 * />
 * 
 * @author AI Reviewer Team
 * @since 2025-12-10
 */
function DocumentUpload({ uploadUrl, onSuccess, onError, acceptedTypes, maxSize }) {
    // ...实现...
}
```

**代码注释**:
```jsx
// ✅ 推荐：关键逻辑添加中英文注释
function MyComponent() {
    // 初始化状态 (Initialize state)
    const [data, setData] = useState([]);
    
    // 加载数据 (Load data)
    useEffect(() => {
        // 发送请求到后端 (Send request to backend)
        loadData().then(result => {
            // 过滤无效数据 (Filter invalid data)
            const validData = result.filter(item => item.isValid);
            setData(validData);
        });
    }, []);
    
    // ...existing code...
}
```

#### 规则 6.8: 兼容性考虑

**使用 React.createElement 作为后备**:

当不能使用 JSX 语法时（如某些动态场景），使用 `React.createElement`:

```javascript
// 场景：动态创建组件
function renderComponent(componentName, props) {
    // ✅ 使用 React.createElement
    return React.createElement(
        window[componentName],
        props,
        props.children
    );
}

// 使用示例
const element = renderComponent('Button', {
    className: 'btn-primary',
    onClick: handleClick,
    children: 'Click Me'
});
```

**在 HTML 中引入 JSX 组件**:
```html
<!-- index.html -->
<!-- 1. 引入 Babel standalone 以支持 JSX -->
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

<!-- 2. JSX 文件必须使用 type="text/babel" -->
<script type="text/babel" src="js/components/MyComponent.jsx"></script>

<!-- 3. 启动应用 -->
<script type="text/babel">
    ReactDOM.render(
        <App />,
        document.getElementById('root')
    );
</script>
```

---

## 🎨 UI主题引擎架构规范

### 规则 7: 数据与UI完全分离架构

**核心理念**: UI只是数据的"壳子"，通过主题引擎实现数据绑定，使得同一套业务逻辑可以渲染成完全不同的UI主题。

#### 规则 7.1: 三层架构设计

**架构分层**:
```
┌─────────────────────────────────────────┐
│      业务数据层 (Data Layer)             │
│  - 页面状态 (Page State)                 │
│  - 业务逻辑 (Business Logic)             │
│  - API调用 (API Calls)                   │
│  ↓ 通过Adapter提供                       │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│   主题渲染引擎 (Theme Rendering Engine)  │
│  - 状态管理 (State Management)           │
│  - Actions注册分发 (Actions Registry)    │
│  - 数据绑定 (Data Binding)               │
│  - UI壳子路由 (UI Shell Routing)         │
│  ↓ 绑定数据到UI                          │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│      UI主题壳子层 (UI Shell Layer)       │
│  - Modern Theme (现代商务)               │
│  - Bubble Theme (梦幻气泡)               │
│  - Anime Theme (二次元动漫)              │
│  - 可无限扩展...                          │
└─────────────────────────────────────────┘
```

#### 规则 7.2: PageAdapter（页面适配器）规范

**❌ 错误做法** - UI和业务逻辑耦合:
```jsx
// 不推荐：UI和逻辑混在一起
function CollaborationPanel() {
    const [activeTab, setActiveTab] = useState('peers');
    const [peers, setPeers] = useState([]);
    
    const loadPeers = async () => {
        const data = await api.getPeers();
        setPeers(data);
    };
    
    return (
        <div>
            <Tabs activeKey={activeTab} onChange={setActiveTab}>
                {/* UI直接耦合 */}
            </Tabs>
        </div>
    );
}
```

**✅ 正确做法** - 使用Adapter提取业务逻辑:
```javascript
// adapters/CollaborationAdapter.js

// 1. 定义初始状态 (Define initial state)
const INITIAL_STATE = {
    activeTab: 'peers',
    peers: [],
    exchanges: [],
    topology: {},
    loading: false,
    error: null,
};

// 2. 定义Actions (Define actions)
export function useCollaborationActions(updateState) {
    const switchTab = useCallback((tabKey) => {
        updateState({ activeTab: tabKey });
    }, [updateState]);
    
    const loadPeers = useCallback(async () => {
        updateState({ loading: true });
        try {
            const peers = await fetchPeers();
            updateState({ peers, loading: false });
        } catch (error) {
            updateState({ error: error.message, loading: false });
        }
    }, [updateState]);
    
    return {
        switchTab,
        loadPeers,
        loadExchanges,
        loadTopology,
    };
}

// 3. 提供统一的绑定Hook (Provide unified binding hook)
export function useCollaborationBinding() {
    const binding = usePageBinding(
        'collaboration',  // 页面ID (Page ID)
        INITIAL_STATE,    // 初始状态 (Initial state)
        {}                // Actions在下面创建 (Actions created below)
    );
    
    const actions = useCollaborationActions(binding.updateState);
    
    return {
        state: binding.state,
        actions: {
            ...binding.actions,
            ...actions,
        },
    };
}
```

**Adapter规范要点**:
```yaml
文件位置: UI/src/adapters/{PageName}Adapter.js
命名规范: {PageName}Adapter.js

必须导出:
  - INITIAL_STATE: 页面初始状态
  - use{PageName}Actions: Actions Hook
  - use{PageName}Binding: 统一绑定Hook

原则:
  ✅ 所有业务逻辑在Adapter中
  ✅ 状态定义清晰完整
  ✅ Actions命名语义化
  ✅ 提供统一的绑定接口
  ❌ 不包含任何UI代码
  ❌ 不依赖特定UI库
```

#### 规则 7.3: UI Shell（UI壳子）规范

**原则**: UI壳子是纯展示层，只负责渲染，所有数据和交互通过绑定获取。

**✅ 正确的UI壳子实现**:
```jsx
// themes/modern/CollaborationShell.jsx

import { useLanguage } from '../../../contexts/LanguageContext';
import { useCollaborationBinding } from '../../../adapters/CollaborationAdapter';

/**
 * 现代主题 - 协作面板UI壳子 (Modern Theme - Collaboration Panel UI Shell)
 * 
 * 纯UI展示层，数据和actions通过引擎绑定
 * (Pure UI presentation layer, data and actions bound via engine)
 */
function ModernCollaborationShell() {
    const { t } = useLanguage();
    
    // 获取数据绑定 (Get data binding)
    const { state, actions } = useCollaborationBinding();
    
    // 纯UI渲染 (Pure UI rendering)
    return (
        <div className="modern-collaboration">
            <Tabs
                activeKey={state.activeTab}
                onChange={actions.switchTab}
                items={tabItems}
            />
        </div>
    );
}

export default ModernCollaborationShell;
```

**UI壳子规范要点**:
```yaml
文件位置: UI/src/themes/{themeName}/{PageName}Shell.jsx
命名规范: {PageName}Shell.jsx

必须遵守:
  ✅ 只使用useXxxBinding获取数据和actions
  ✅ 不直接调用API
  ✅ 不直接管理状态（除UI专属状态）
  ✅ 通过actions触发操作
  ✅ 可以有完全不同的UI结构
  ❌ 不包含业务逻辑
  ❌ 不直接修改state
  ❌ 不与其他UI壳子耦合
```

#### 规则 7.4: 主题注册规范

**在UIThemeEngineContext中注册主题**:
```javascript
// contexts/UIThemeEngineContext.jsx

export const UI_THEMES = {
    // 现代主题 (Modern Theme)
    modern: {
        id: 'modern',
        name: {
            zh: '现代商务',
            en: 'Modern Business',
        },
        description: {
            zh: '简洁专业的现代化商务风格',
            en: 'Clean and professional modern business style',
        },
        // 页面UI壳子映射 (Page UI shell mapping)
        shellMapping: {
            collaboration: () => import('../../themes/modern/CollaborationShell'),
            qa: () => import('../../themes/modern/QAShell'),
            documents: () => import('../../themes/modern/DocumentsShell'),
            // 可继续添加更多页面 (Can add more pages)
        },
        config: {
            layout: 'sidebar',
            animation: 'smooth',
            density: 'comfortable',
        },
        status: 'active',
    },
    
    // 气泡主题 (Bubble Theme)
    bubble: {
        id: 'bubble',
        name: {
            zh: '梦幻气泡',
            en: 'Dreamy Bubble',
        },
        description: {
            zh: '可爱梦幻的气泡风格界面',
            en: 'Cute and dreamy bubble-style interface',
        },
        shellMapping: {
            collaboration: () => import('../../themes/bubble/CollaborationShell'),
            // 其他页面... (Other pages...)
        },
        config: {
            layout: 'floating',
            animation: 'bouncy',
            density: 'spacious',
        },
        status: 'active',
    },
};
```

**主题注册规范**:
```yaml
必须字段:
  - id: 主题唯一标识
  - name: 多语言名称 {zh, en}
  - description: 多语言描述 {zh, en}
  - shellMapping: 页面到UI壳子的映射
  - config: 主题配置
  - status: 主题状态 (active/developing)

shellMapping规范:
  - 键: 页面ID (pageId)
  - 值: 返回动态import的函数
  - 懒加载: 使用() => import()语法
  
示例:
  shellMapping: {
    collaboration: () => import('../../themes/bubble/CollaborationShell'),
  }
```

#### 规则 7.5: 页面集成规范

**使用EnginePageRouter集成主题引擎**:
```jsx
// components/collaboration/CollaborationPanel.jsx

import { EnginePageRouter } from '../../engine/PageRouter';

/**
 * 回退组件 (Fallback component)
 * 当主题不支持该页面时使用
 * (Used when theme doesn't support this page)
 */
function TraditionalCollaborationPanel() {
    // 传统UI实现 (Traditional UI implementation)
    return <div>{/* Traditional UI */}</div>;
}

/**
 * 协作面板 - 主题引擎驱动 (Collaboration Panel - Theme Engine Driven)
 * 
 * 根据当前主题动态加载对应的UI壳子
 * (Dynamically loads corresponding UI shell based on current theme)
 */
function CollaborationPanel() {
    return (
        <EnginePageRouter
            pageId="collaboration"
            fallbackComponent={TraditionalCollaborationPanel}
        />
    );
}

export default CollaborationPanel;
```

**页面集成规范**:
```yaml
使用方式:
  1. 创建回退组件 (TraditionalXxxPanel)
  2. 使用EnginePageRouter包装
  3. 传入pageId和fallbackComponent

参数说明:
  - pageId: 页面唯一标识，需与Adapter和主题配置中的ID一致
  - fallbackComponent: 回退组件，主题不支持时使用

工作流程:
  1. PageRouter获取当前主题配置
  2. 查找shellMapping[pageId]
  3. 动态导入对应的UI壳子
  4. 渲染UI壳子（自动获取数据绑定）
  5. 如果主题不支持，使用fallbackComponent
```

#### 规则 7.6: 数据流动规范

**数据流向**:
```
用户操作 → UI壳子触发action → 引擎更新状态 → UI壳子重新渲染
```

**示例流程**:
```javascript
// 1. 用户点击切换Tab
用户点击"交换历史"
  ↓
  
// 2. UI壳子触发action (UI shell triggers action)
<Button onClick={() => actions.switchTab('exchange')} />
  ↓
  
// 3. 引擎更新状态 (Engine updates state)
updatePageState('collaboration', { activeTab: 'exchange' })
  ↓
  
// 4. 状态改变 (State changes)
state.activeTab = 'exchange'
  ↓
  
// 5. UI壳子重新渲染 (UI shell re-renders)
{state.activeTab === 'exchange' && <ExchangeHistory data={state.exchanges} />}
```

**关键原则**:
```yaml
✅ 正确做法:
  - UI壳子只读取state
  - 通过actions修改状态
  - 状态更新通过引擎

❌ 错误做法:
  - UI壳子直接修改state
  - 绕过actions直接调用API
  - 在UI壳子中管理业务状态
```

#### 规则 7.7: 添加新主题的步骤

**完整步骤**:
```yaml
步骤1: 创建UI壳子
  位置: UI/src/themes/{themeName}/{PageName}Shell.jsx
  
  示例:
    // themes/anime/CollaborationShell.jsx
    function AnimeCollaborationShell() {
        const { state, actions } = useCollaborationBinding();
        return (
            <div className="anime-layout">
                {/* 动漫风格的UI实现 */}
            </div>
        );
    }

步骤2: 创建主题样式
  位置: UI/src/themes/{themeName}/{themeName}-{pageName}.css
  
  示例:
    // themes/anime/anime-collaboration.css
    .anime-layout {
        background: url('/anime-bg.jpg');
        /* 动漫风格样式 */
    }

步骤3: 注册主题
  位置: UI/src/contexts/UIThemeEngineContext.jsx
  
  添加:
    anime: {
        id: 'anime',
        name: { zh: '二次元动漫', en: 'Anime Style' },
        shellMapping: {
            collaboration: () => import('../../themes/anime/CollaborationShell'),
        },
        status: 'active',
    }

完成！新主题立即可用，无需修改业务代码！
```

#### 规则 7.8: 主题开发最佳实践

**设计原则**:
```yaml
1. 数据驱动 (Data-Driven):
   - UI只负责展示
   - 所有数据来自绑定
   - 通过actions触发交互

2. 完全独立 (Completely Independent):
   - 不同主题互不影响
   - 可以有完全不同的布局结构
   - 可以有完全不同的交互方式

3. 性能优化 (Performance Optimization):
   - 使用懒加载
   - 代码分割
   - 状态复用

4. 回退机制 (Fallback Mechanism):
   - 必须提供回退组件
   - 保证功能始终可用
   - 优雅降级
```

**避免的陷阱**:
```yaml
❌ 在UI壳子中包含业务逻辑
❌ UI壳子之间相互依赖
❌ 直接修改state而不通过actions
❌ 硬编码API调用
❌ 跳过Adapter直接管理状态
❌ 忘记提供回退组件
```

#### 规则 7.9: 目录结构规范

**完整目录结构**:
```
UI/src/
├── engine/                           (主题引擎核心)
│   ├── ThemeRenderEngine.jsx         (渲染引擎)
│   └── PageRouter.jsx                (页面路由器)
│
├── adapters/                         (页面适配器)
│   ├── CollaborationAdapter.js       (协作面板适配器)
│   ├── QAAdapter.js                  (问答面板适配器)
│   └── DocumentsAdapter.js           (文档管理适配器)
│
├── themes/                           (主题UI壳子)
│   ├── modern/                       (现代主题)
│   │   ├── CollaborationShell.jsx
│   │   ├── QAShell.jsx
│   │   └── modern-*.css
│   │
│   ├── bubble/                       (气泡主题)
│   │   ├── CollaborationShell.jsx
│   │   ├── QAShell.jsx
│   │   └── bubble-*.css
│   │
│   └── anime/                        (动漫主题)
│       ├── CollaborationShell.jsx
│       └── anime-*.css
│
├── contexts/                         (上下文)
│   ├── UIThemeEngineContext.jsx      (主题引擎上下文)
│   ├── LanguageContext.jsx           (语言上下文)
│   └── ThemeContext.jsx              (颜色主题上下文)
│
└── components/                       (组件)
    ├── collaboration/
    │   └── CollaborationPanel.jsx    (使用EnginePageRouter)
    └── ...
```

#### 规则 7.10: 性能优化指南

**懒加载优化**:
```javascript
// ✅ 正确：使用动态import
shellMapping: {
    collaboration: () => import('../../themes/bubble/CollaborationShell'),
}

// 好处：
// 1. 只有在使用该主题时才加载UI壳子
// 2. 减少初始bundle大小
// 3. 提升首屏加载速度
```

**状态复用**:
```javascript
// 切换主题时，状态不丢失
// 新UI壳子自动获取已有的状态

// 用户在Modern主题操作
state.activeTab = 'exchange';
state.peers = [...已加载的数据];

// 切换到Bubble主题
// 新UI壳子自动获取相同的state
const { state, actions } = useCollaborationBinding();
// state.activeTab依然是'exchange'
// state.peers依然是已加载的数据
```

**代码分割**:
```javascript
// 每个主题的UI壳子独立打包
modern/  → modern-chunk.js
bubble/  → bubble-chunk.js
anime/   → anime-chunk.js

// 只加载当前使用的主题
```

---

### 8. 样式管理规范

#### 规则 8.1: 样式文件组织

**原则**: 所有样式必须提取到独立的 CSS 文件中，禁止使用内联样式（特殊情况除外）

```yaml
样式文件位置: src/main/resources/static/assets/css/

目录结构:
  src/main/resources/static/assets/css/
    ├── reset.css              (重置样式)
    ├── main.css               (全局样式)
    ├── qa-tab.css             (问答 Tab)
    ├── documents-tab.css      (文档管理 Tab)
    ├── statistics-tab.css     (统计 Tab)
    ├── ai-analysis.css        (AI 分析面板)
    ├── hope-dashboard.css     (HOPE 仪表盘)
    ├── welcome-guide.css      (引导页面)
    └── ...                    (其他模块样式)

命名规范:
  - 文件名用小写字母和连字符 (kebab-case)
  - 一个模块一个 CSS 文件
  - 文件名与组件名对���
```

#### 规则 7.2: 禁止内联样式

**❌ 错误做法** - 使用内联样式:
```jsx
// 不推荐：内联样式难以维护和复用
<div style={{
    padding: '10px 15px',
    margin: '10px 0',
    background: 'linear-gradient(135deg, #667eea15 0%, #764ba215 100%)',
    border: '1px solid #667eea40',
    borderRadius: '8px',
    fontSize: '14px',
    color: '#667eea'
}}>
    提示信息
</div>
```

**✅ 正确做法** - 使用 CSS 类:
```jsx
// 推荐：使用 CSS 类，易于维护和复用
<div className="filter-hint">
    提示信息
</div>
```

**CSS 文件** (`documents-tab.css`):
```css
/* 实时过滤提示样式 (Real-time Filter Hint) */
.filter-hint {
    padding: 10px 15px;
    margin: 10px 0;
    background: linear-gradient(135deg, #667eea15 0%, #764ba215 100%);
    border: 1px solid #667eea40;
    border-radius: 8px;
    font-size: 14px;
    color: #667eea;
}
```

#### 规则 7.3: 允许内联样式的特殊情况

**仅在以下情况允许使用内联样式**:

1. **动态计算的值** (需要根据 props 或 state 动态改变)
   ```jsx
   <div style={{ width: `${progress}%` }}>进度条</div>
   <div style={{ left: `${position}px` }}>拖拽元素</div>
   ```

2. **一次性使用的临时样式** (调试或 POC 阶段)
   ```jsx
   // ⚠️ 仅用于快速原型，最终必须提取到 CSS
   <div style={{ border: '1px solid red' }}>调试边框</div>
   ```

3. **第三方库要求的样式** (某些库必须通过 style prop 传递)
   ```jsx
   <ThirdPartyComponent style={requiredStyles} />
   ```

**注意**: 即使使用内联样式，也应在代码审查时评估是否可以提取到 CSS。

#### 规则 7.4: CSS 类命名规范

**使用 BEM 命名法** (Block-Element-Modifier):

```css
/* Block (块) */
.documents-filter-hint { }

/* Element (元素) */
.documents-filter-hint__icon { }
.documents-filter-hint__message { }

/* Modifier (修饰符) */
.documents-filter-hint--warning { }
.documents-filter-hint--error { }
.documents-filter-hint--success { }
```

**命名规则**:
- 使用小写字母和连字符 (kebab-case)
- 类名应具有语义化，描述用途而非样式
- 避免使用缩写（除非是通用缩写如 btn、nav）
- 模块前缀 + 功能描述

**示例**:
```css
/* ✅ 好的命名 */
.documents-upload-area { }
.search-filter-container { }
.ai-analysis-panel { }

/* ❌ 不好的命名 */
.blue-box { }          /* 描述样式而非用途 */
.div1 { }              /* 无意义 */
.temp { }              /* 过于笼统 */
```

#### 规则 7.5: CSS 文件结构

**每个 CSS 文件应遵循统一的结构**:

```css
/**
 * 模块名称样式文件 (Module Name Styles)
 * 
 * @author AI Reviewer Team
 * @since 2025-12-10
 */

/* ============================================================================
   全局样式 (Global Styles)
   ============================================================================ */
.module-container {
    /* ... */
}

/* ============================================================================
   头部样式 (Header Styles)
   ============================================================================ */
.module-header {
    /* ... */
}

/* ============================================================================
   内容区域样式 (Content Area Styles)
   ============================================================================ */
.module-content {
    /* ... */
}

/* ============================================================================
   底部样式 (Footer Styles)
   ============================================================================ */
.module-footer {
    /* ... */
}

/* ============================================================================
   响应式样式 (Responsive Styles)
   ============================================================================ */
@media (max-width: 768px) {
    .module-container {
        /* 移动端样式 */
    }
}
```

**结构规范**:
- ✅ 文件头部包含注释说明
- ✅ 使用分隔线将不同功能区域分组
- ✅ 每组样式有中英文注释
- ✅ 响应式样式放在文件末尾

#### 规则 7.6: 样式复用与变量

**使用 CSS 变量管理主题色**:

```css
/* 在 main.css 或 root 中定义全局变量 */
:root {
    /* 主题色 (Theme Colors) */
    --primary-color: #667eea;
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    
    /* 间距 (Spacing) */
    --spacing-xs: 5px;
    --spacing-sm: 10px;
    --spacing-md: 15px;
    --spacing-lg: 20px;
    
    /* 圆角 (Border Radius) */
    --radius-sm: 4px;
    --radius-md: 8px;
    --radius-lg: 12px;
}

/* 在具体样式中使用变量 */
.filter-hint {
    padding: var(--spacing-sm) var(--spacing-md);
    margin: var(--spacing-sm) 0;
    background: var(--primary-gradient);
    border-radius: var(--radius-md);
    color: var(--primary-color);
}
```

#### 规则 7.7: 动画规范

**动画应定义在 CSS 中，使用 @keyframes**:

```css
/* ✅ 正确：在 CSS 中定义动画 */
.filter-hint {
    animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-5px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

**避免在 JS 中操作样式**:
```javascript
// ❌ 错误：在 JS 中直接操作样式
element.style.opacity = '0';
element.style.transform = 'translateY(-5px)';

// ✅ 正确：使用 CSS 类切换
element.classList.add('fade-in');
```

---

## 📋 代码质量检查清单

### 每次提交代码前必须检查

#### 后端代码 (Backend Code)
- [ ] **注释格式**：所有类、方法、复杂逻辑都有 `中文(英文)` 注释
- [ ] **日志国际化**：所有日志使用 `I18n.get()` 而不是硬编码
- [ ] **常量提取**：所有字符串常量提取到 YAML 文件
- [ ] **YAML 文件**：已创建对应的 `{模块名}.yml` 文件
- [ ] **中英文版本**：YAML 文件包含中英文两个版本，用 `---` 分隔
- [ ] **编译通过**：`mvn compile` 无错误
- [ ] **测试通过**：相关单元测试通过
- [ ] **代码格式**：符合 Java 代码规范

#### 前端代码 (Frontend Code)
- [ ] **⭐ JSX 优先**：新组件必须使用 JSX 格式实现，避免纯 JavaScript 创建 DOM
- [ ] **文件命名**：组件文件使用 `.jsx` 扩展名，PascalCase 命名（如 `MyComponent.jsx`）
- [ ] **组件结构**：遵循标准 JSX 组件结构（imports → state → effects → handlers → render）
- [ ] **Props 解构**：在函数参数中解构 props，使用清晰的命名
- [ ] **事件处理**：事件处理函数使用 `handle` 前缀，传递给子组件用 `on` 前缀
- [ ] **条件渲染**：使用 `&&`、三元运算符或函数，避免复杂的内联逻辑
- [ ] **列表渲染**：使用 `map` 且必须提供 `key` 属性（使用唯一 ID，避免 index）
- [ ] **样式提取**：所有样式提取到 CSS 文件，避免内联样式
- [ ] **CSS 文件**：已创建或更新对应的 CSS 文件
- [ ] **类名规范**：CSS 类名使用 BEM 命名法，语义化命名
- [ ] **注释完整**：组件有文件头注释，关键逻辑有中英文注释
- [ ] **国际化**：前端文本使用 `t()` 函数，避免硬编码
- [ ] **动画定义**：动画在 CSS 中定义，使用 @keyframes
- [ ] **变量使用**：使用 CSS 变量管理主题色和常用值
- [ ] **响应式**：考虑移动端适配，添加媒体查询

---

## 🔄 新 Session 启动检查清单

### 每次打开新 Session 时

1. **告诉 Copilot 遵守规范**：
   ```
   "遵守 20251209-23-00-00-CODE_STANDARDS.md 中的所有代码规范"
   ```

2. **验证 Copilot 理解**：
   ```
   "生成一个示例类，展示正确的注释格式和国际化用法"
   ```

3. **确认规范已应用**：
   - ✅ 注释格式正确
   - ✅ 日志使用 I18n
   - ✅ 字符串已国际化

---

## 📚 相关文档

- **实施计划**：`20251209-22-29-00-IMPLEMENTATION_PLAN.md`
- **会话恢复**：`SESSION_RECOVERY_GUIDE.md`
- **目录说明**：`README.md`

---

## ✅ 规范版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.3 | 2025-12-10 21:20 | 🎯 新增规则 6：JSX 优先规范（6.1-6.8），明确前端代码必须使用 JSX 实现 |
| v1.2 | 2025-12-10 21:15 | 新增前端样式管理规范（规则 7.1-7.7），禁止内联样式，统一 CSS 管理 |
| v1.1 | 2025-12-09 23:30 | 优化文件组织：按语言分目录（zh/和en/），添加老代码兼容 |
| v1.0 | 2025-12-09 23:00 | 初始版本，建立代码规范 |

---

**这是 RAG 2.0 项目的核心规范，所有开发人员必须遵守！** 🎯

---

**文档版本**: v1.3  
**创建日期**: 2025-12-09 23:00:00  
**最后更新**: 2025-12-10 21:20:00  
**状态**: ✅ 生效中  
**维护者**: AI Reviewer Team

