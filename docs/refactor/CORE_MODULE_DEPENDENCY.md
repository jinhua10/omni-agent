# OmniAgent Core 模块依赖关系图

**更新时间**: 2025-12-15  
**当前版本**: 1.0.1  
**模块总数**: 42个（已实现）

## 模块依赖结构（七维可插拔架构）✨

> **架构理念**: 基于 Spring Boot Starter 模式的**七维可插拔架构**  
> **设计原则**: 依赖倒置 • 接口隔离 • 编译时选择 • 运行时注入  
> **架构完成度**: ✅ **100%** (7/7维度全部实现)

```
                         ┌─────────────────────────────────────────┐
                         │        用户应用 (Application)           │
                         │    通过依赖选择组合不同的实现           │
                         └────────────────┬────────────────────────┘
                                          │
         ┌────────────────────────────────┼────────────────────────────────┐
         │                                │                                │
    ┌────▼────┐   ┌────▼────┐   ┌────▼────┐   ┌────▼────┐   ┌────▼────┐
    │持久化   │   │文档存储 │   │检索引擎 │   │P2P协作  │   │投票仲裁 │
    │Starter  │   │Starter  │   │Starter  │   │Starter  │   │Starter  │
    └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘
         │             │              │             │              │
         └─────────────┼──────────────┼─────────────┼──────────────┘
                       │              │             │
                       │              │             │
                 ┌─────▼──────────────▼─────────────▼──────┐
                 │       omni-agent-core                   │
                 │       (核心业务逻辑层)                   │
                 │     只依赖接口，不依赖实现               │
                 └─────┬──────────────┬──────────────┬─────┘
                       │              │              │
        ┌──────────────┼──────────────┼──────────────┼──────────────┐
        │              │              │              │              │
┌───────▼────────┐ ┌──▼──────┐ ┌────▼──────┐ ┌─────▼─────┐ ┌─────▼──────┐
│  核心业务域    │ │文档处理 │ │交互增强域 │ │ P2P协作域 │ │ 智能分析域 │
│                │ │         │ │           │ │           │ │            │
│ • HOPE知识     │ │•文档分块│ │•查询处理  │ │•端点发现  │ │•行为分析   │
│ • 角色系统     │ │•图像提取│ │•反馈系统  │ │•安全握手  │ │•态度推断   │
│ • 概念演化     │ │•PPL引擎 │ │•投票系统  │ │•数据传输  │ │•热度计算   │
└────────┬───────┘ └──┬──────┘ └────┬──────┘ └─────┬─────┘ └─────┬──────┘
         │             │              │              │              │
         └─────────────┼──────────────┼──────────────┼──────────────┘
                       │              │              │
                 ┌─────▼──────────────▼──────────────▼──────┐
                 │       API 接口层 (抽象)                  │
                 │     定义标准规范，不含实现               │
                 └─────┬──────────────┬──────────────┬─────┘
                       │              │              │
    ┌──────────────────┼──────────────┼──────────────┼──────────────┐
    │                  │              │              │              │
┌───▼────────┐  ┌─────▼──────┐  ┌───▼──────┐  ┌───▼──────┐
│Persistence │  │Document    │  │RAG API   │  │AI API    │
│   API      │  │Storage API │  │(检索)    │  │(推理)    │
│  (持久化)  │  │(文档存储)  │  │          │  │          │
└─────┬──────┘  └─────┬──────┘  └────┬─────┘  └────┬─────┘
      │               │               │              │
      │🔌维度1        │🔌维度2        │🔌维度3       │🔌维度4
      │(结构化)       │(文档)         │(检索)        │(AI)
      │               │               │              │

┌───▼──────┐  ┌───▼──────┐  ┌───▼──────┐
│P2P API   │  │Voting    │  │Behavior  │
│(协作)    │  │  API     │  │   API    │
│          │  │(投票)    │  │(行为分析)│
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │              │              │
     │🔌维度5       │🔌维度6       │🔌维度7
     │(P2P)         │(投票)        │(行为)
     │              │              │
┌─────▼──────┐  ┌─────▼──────┐  ┌────▼─────┐  ┌────▼─────┐  ┌────▼─────┐  ┌────▼─────┐  ┌────▼─────┐
│持久化      │  │文档存储    │  │RAG       │  │P2P       │  │Voting    │  │Behavior  │  │AI        │
│Starters    │  │Starters    │  │Starters  │  │Starters  │  │Starters  │  │Starters  │  │Starters  │
│(选1)       │  │(选1)       │  │(选1)     │  │(选1)     │  │(选1)     │  │(选1)     │  │(选1)     │
│            │  │            │  │          │  │          │  │          │  │          │  │          │
│✅ memory   │  │✅ file     │  │✅ file   │  │✅ memory │  │✅ memory │  │✅ memory │  │✅ ollama │
│✅ h2       │  │✅ mongodb  │  │✅ h2     │  │✅ h2     │  │✅ redis  │  │          │  │✅ online │
│✅ sqlite   │  │✅ redis    │  │✅ sqlite │  │✅ sqlite │  │✅ mongodb│  │          │  │          │
│✅ redis    │  │✅ elastic  │  │✅ redis  │  │✅ redis  │  │✅ elastic│  │          │  │          │
│✅ mongodb  │  │✅ s3       │  │✅ mongodb│  │✅ mongodb│  │          │  │          │  │          │
│✅ elastic  │  │✅ minio    │  │✅ elastic│  │✅ elastic│  │          │  │          │  │          │
│            │  │            │  │          │  │          │  │          │  │          │  │          │
│6种实现     │  │6种实现     │  │6种实现   │  │6种实现   │  │4种实现   │  │1种实现   │  │2种实现   │
│100%完成    │  │100%完成    │  │100%完成  │  │100%完成  │  │100%完成  │  │100%完成  │  │100%完成  │
└────────────┘  └────────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘



═══════════════════════════════════════════════════════════════════════════

                         架构关键特性说明 (七维架构)

  🎯 依赖倒置原则 (DIP)
     Core → 依赖 → 接口 API
     Starter → 实现 → 接口 API
     高层不依赖低层，都依赖抽象

  🔌 编译时选择
     在 pom.xml 中选择需要的 Starter
     <dependency>
       <artifactId>omni-agent-persistence-starter-elasticsearch</artifactId>
     </dependency>

  🚀 运行时自动配置
     Spring Boot 根据类路径自动注入实现
     @Autowired
     private QuestionClassifierPersistence persistence; // 自动注入

  🎨 灵活组合 (七维架构)
     7 个维度独立选择，任意组合：
     • 持久化: 6 种选择 ✅
     • 文档存储: 6 种选择 ✅
     • RAG: 6 种选择 ✅
     • AI: 2 种选择 ✅
     • P2P协作: 6 种选择 ✅ **NEW**
     • 投票仲裁: 4 种选择 ✅ **NEW**
     • 行为分析: 1 种选择 ✅ **NEW**
     = 6 × 6 × 6 × 2 × 6 × 4 × 1 = 10,368 种组合！

  💡 切换无需改代码
     只需修改 pom.xml 依赖和 application.yml 配置
     业务代码保持不变

  🆕 新增三大维度 (2025-12-15)
     • P2P协作: 端点发现、安全握手、数据传输
     • 投票仲裁: 多方投票、冲突解决、民主决策
     • 行为分析: 用户信号、态度推断、热度计算

═══════════════════════════════════════════════════════════════════════════
```

---

## 七维可插拔架构使用示例

### 场景 1: 个人开发者（快速启动）

**pom.xml 依赖配置**:
```xml
<dependencies>
    <!-- 维度1: 持久化 - 内存（无需外部服务） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-persistence-starter-memory</artifactId>
    </dependency>
    
    <!-- 维度2: 文档存储 - 本地文件（简单快速） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-document-storage-starter-file</artifactId>
    </dependency>
    
    <!-- 维度3: RAG - Lucene本地（无需外部服务） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-rag-starter-file</artifactId>
    </dependency>
    
    <!-- 维度4: AI - Ollama（离线可用） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-ai-starter-ollama</artifactId>
    </dependency>
    
    <!-- 维度5: P2P协作 - 内存（本地测试） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-p2p-starter-memory</artifactId>
    </dependency>
    
    <!-- 维度6: 投票仲裁 - 内存（快速验证） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-voting-starter-memory</artifactId>
    </dependency>
    
    <!-- 维度7: 行为分析 - 内存（实时分析） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-behavior-starter-memory</artifactId>
    </dependency>
</dependencies>
```

**application.yml 配置**:
```yaml
omni-agent:
  persistence:
    type: memory
  document-storage:
    type: file
    file:
      base-path: ./data/storage
  rag:
    type: file
    file:
      index-path: ./data/lucene
  ai:
    type: ollama
    ollama:
      base-url: http://localhost:11434
  p2p:
    type: memory
    node-id: dev-node-001
  voting:
    type: memory
    default-threshold: 0.6
  behavior:
    type: memory
    cache-enabled: true
```

**特点**: 
✅ 零外部依赖 ✅ 快速启动 ✅ 适合开发调试  
✅ P2P协作支持 ✅ 投票机制 ✅ 行为分析

---

### 场景 2: 小型团队（混合部署）

**pom.xml 依赖配置**:
```xml
<dependencies>
    <!-- 维度1: 持久化 - H2数据库（嵌入式） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-persistence-starter-h2</artifactId>
    </dependency>
    
    <!-- 维度2: 文档存储 - MongoDB（适合文档） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-document-storage-starter-mongodb</artifactId>
    </dependency>
    
    <!-- 维度3: RAG - Elasticsearch（生产级） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-rag-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度4: AI - 在线API（统一管理） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-ai-starter-online-api</artifactId>
    </dependency>
    
    <!-- 维度5: P2P协作 - Redis（分布式协作） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-p2p-starter-redis</artifactId>
    </dependency>
    
    <!-- 维度6: 投票仲裁 - MongoDB（持久化投票） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-voting-starter-mongodb</artifactId>
    </dependency>
    
    <!-- 维度7: 行为分析 - 内存（实时性能） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-behavior-starter-memory</artifactId>
    </dependency>
</dependencies>
```

**application.yml 配置**:
```yaml
omni-agent:
  persistence:
    type: h2
    h2:
      url: jdbc:h2:./data/omni-agent
  document-storage:
    type: mongodb
    mongodb:
      uri: mongodb://localhost:27017/omni-agent
  rag:
    type: elasticsearch
    elasticsearch:
      hosts: localhost:9200
  ai:
    type: online-api
    online-api:
      provider: openai
      api-key: ${OPENAI_API_KEY}
  p2p:
    type: redis
    redis:
      host: localhost
      port: 6379
  voting:
    type: mongodb
    mongodb:
      database: voting
  behavior:
    type: memory
```

**特点**: ✅ 结构化数据用H2 ✅ 文档/图像用MongoDB ✅ 检索用ES ✅ AI在线API ✅ 分布式协作

---

### 场景 3: 企业生产（云原生）

**pom.xml 依赖配置**:
```xml
<dependencies>
    <!-- 维度1: 持久化 - Elasticsearch集群 -->
    <dependency>
        <artifactId>omni-agent-persistence-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度2: 文档存储 - AWS S3（高可靠） -->
    <dependency>
        <artifactId>omni-agent-document-storage-starter-s3</artifactId>
    </dependency>
    
    <!-- 维度3: RAG - Elasticsearch（同一集群） -->
    <dependency>
        <artifactId>omni-agent-rag-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度4: AI - 在线API（企业级） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-ai-starter-online-api</artifactId>
    </dependency>
    
    <!-- 维度5: P2P - Elasticsearch（分布式） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-p2p-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度6: Voting - Elasticsearch（持久化） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-voting-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度7: Behavior - Memory（实时） -->
    <dependency>
        <groupId>top.yumbo.ai.omni</groupId>
        <artifactId>omni-agent-behavior-starter-memory</artifactId>
    </dependency>
</dependencies>
```

**application.yml 配置**:
```yaml
omni-agent:
  persistence:
    type: elasticsearch
    elasticsearch:
      hosts: es-cluster:9200
  document-storage:
    type: s3
    s3:
      bucket: omni-agent-docs
      region: us-east-1
  rag:
    type: elasticsearch
    elasticsearch:
      hosts: es-cluster:9200
      index: omni-agent-rag
  ai:
    type: online-api
    online-api:
      provider: openai
      api-key: ${OPENAI_API_KEY}
      model: gpt-4
  p2p:
    type: elasticsearch
    node-id: prod-node-${INSTANCE_ID}
  voting:
    type: elasticsearch
    default-threshold: 0.7
  behavior:
    type: memory
    cache-enabled: true
```

**特点**: ✅ 高可用 ✅ 可扩展 ✅ 云托管 ✅ 企业级 ✅ 全七维配置

---

### 切换场景：从开发到生产

**业务代码（无需修改）**:
```java
@Service
public class DocumentService {
    
    // 注入接口，不关心具体实现
    @Autowired
    private QuestionClassifierPersistence persistence;
    
    @Autowired
    private DocumentStorageService storageService;
    
    @Autowired
    private RAGService ragService;
    
    @Autowired
    private AIService aiService;
    
    public void processDocument(Document doc) {
        // 业务逻辑保持不变
        // Spring Boot 会根据选择的 Starter 自动注入对应实现
        
        // 存储元数据（可能是 Memory、H2、ES...）
        persistence.saveQuestionType(config);
        
        // 存储分块（可能是 File、MongoDB、S3...）
        storageService.saveChunk(doc.getId(), chunk);
        
        // 索引文档（可能是 Lucene、ES...）
        ragService.indexDocument(doc);
        
        // AI 处理（可能是 Ollama、OpenAI...）
        aiService.process(request);
    }
}
```

**只需修改**:
1. `pom.xml` - 更换依赖
2. `application.yml` - 更新配置
3. 业务代码 - 无需修改！✅

---

## 模块调用链路

### 1. 查询处理链路
```
用户查询
  │
  ├─> QueryProcessor (查询处理器)
  │     │
  │     ├─> HOPEKnowledgeManager (HOPE 知识管理)
  │     │     │
  │     │     ├─> HighFrequencyLayerService (高频层)
  │     │     ├─> OrdinaryLayerService (普通层)
  │     │     └─> PermanentLayerService (永久层)
  │     │
  │     ├─> DocumentChunker (文档分块)
  │     │     │
  │     │     └─> ChunkingStrategy (分块策略)
  │     │           ├─> SimpleChunkingStrategy
  │     │           ├─> PPLChunkingStrategy
  │     │           └─> LLMChunkingStrategy
  │     │
  │     └─> RoleDetector (角色检测)
  │           │
  │           └─> AIRoleAnalyzer (AI 角色分析)
  │
  └─> 返回 SearchResult
```

### 2. 文档索引链路
```
文档上传
  │
  ├─> DocumentUtils (文档工具)
  │     │
  │     ├─> DocumentImageExtractor (图像提取)
  │     │     ├─> PDFImageExtractor
  │     │     ├─> WordImageExtractor
  │     │     ├─> ExcelImageExtractor
  │     │     └─> PowerPointImageExtractor
  │     │
  │     └─> DocumentChunker (文档分块)
  │           │
  │           ├─> ChunkStorageService (分块存储)
  │           └─> PPLService (PPL 处理)
  │
  ├─> ConceptExtractor (概念提取)
  │     │
  │     └─> HOPEConceptExtractor (HOPE 概念提取)
  │
  └─> 存储到索引
```

### 3. 知识演化链路
```
知识冲突检测
  │
  ├─> ConflictDetector (冲突检测器)
  │     │
  │     └─> SimilarityCalculator (相似度计算)
  │
  ├─> VotingService (投票服务)
  │     │
  │     ├─> VotingSession (投票会话)
  │     └─> VotingArbiter (投票仲裁)
  │
  └─> ConceptEvolutionService (概念演化)
        │
        └─> ConceptUpdater (概念更新)
              │
              └─> VersionManager (版本管理)
```

### 4. 反馈处理链路
```
用户反馈
  │
  ├─> FeedbackCollector (反馈收集)
  │     │
  │     ├─> BehaviorAnalyzer (行为分析)
  │     │     │
  │     │     └─> SignalAggregator (信号聚合)
  │     │
  │     └─> AttitudeInferenceEngine (态度推理)
  │
  ├─> DocumentWeightService (文档权重)
  │
  └─> QARecordService (QA 记录)
```

---

## 模块间依赖强度

### 强依赖（紧耦合）
- `hope` ← → `persistence` (持久化是 HOPE 的核心依赖)
- `chunking` ← → `ppl` (分块严重依赖 PPL)
- `role` ← → `concept` (角色系统依赖概念管理)

### 中依赖（适度耦合）
- `query` ← → `hope` (查询使用 HOPE 增强)
- `evolution` ← → `voting` (演化需要投票机制)
- `service` ← → `ai` (业务服务调用 AI 引擎)

### 弱依赖（松耦合）
- `feedback` ← → `gamification` (反馈触发游戏化)
- `archive` ← → `query` (归档存储查询历史)
- `monitor` ← → `quality` (监控关注质量指标)

---

## 建议的重构模块划分

### 方案 1: 按业务域划分
```
omni-agent-knowledge (知识管理域)
  ├─ hope (HOPE 系统)
  ├─ concept (概念管理)
  ├─ evolution (演化系统)
  └─ role (角色系统)

omni-agent-document (文档处理域)
  ├─ chunking (文档分块)
  ├─ image (图像处理)
  └─ ppl (PPL 引擎)

omni-agent-interaction (交互域)
  ├─ query (查询系统)
  ├─ feedback (反馈系统)
  ├─ voting (投票系统)
  └─ service (业务服务)

omni-agent-infrastructure (基础设施域)
  ├─ persistence (持久化) → 已独立
  ├─ config (配置管理)
  ├─ ai (AI 引擎)
  └─ util (工具类)
```

### 方案 2: 按技术层次划分
```
omni-agent-domain (领域层)
  ├─ model (领域模型)
  ├─ repository (仓库接口)
  └─ service (领域服务)

omni-agent-application (应用层)
  ├─ query (查询应用)
  ├─ command (命令应用)
  └─ integration (集成服务)

omni-agent-infrastructure (基础设施层)
  ├─ persistence (持久化实现)
  ├─ ai (AI 集成)
  └─ monitoring (监控)

omni-agent-common (公共层)
  ├─ util (工具类)
  ├─ config (配置)
  └─ i18n (国际化)
```

---

## 重构优先级建议

### ✅ 已完成的重构（2025-12-15）
1. ✅ **持久化模块分离** - `omni-agent-persistence-api` + 6个Starter
   - 状态: 完成
   - 成果: 完全解耦，支持Memory/H2/SQLite/Redis/MongoDB/ES

2. ✅ **RAG 接口标准化** - `omni-agent-rag-api` + 6个Starter
   - 状态: 完成
   - 成果: 统一RAG接口，支持File/H2/SQLite/Redis/MongoDB/ES

3. ✅ **文档存储接口标准化** - `omni-agent-document-storage-api` + 6个Starter
   - 状态: 完成
   - 成果: 支持File/MongoDB/Redis/ES/S3/MinIO

4. ✅ **AI 接口标准化** - `omni-agent-ai-api` + 2个Starter
   - 状态: 完成
   - 成果: 支持Ollama/Online-API

5. ✅ **P2P 协作层** - `omni-agent-p2p-api` + 6个Starter ⭐
   - 状态: 完成
   - 成果: 端点发现、安全握手、数据传输

6. ✅ **投票仲裁层** - `omni-agent-voting-api` + 4个Starter ⭐
   - 状态: 完成
   - 成果: 多角色投票、冲突解决

7. ✅ **行为分析层** - `omni-agent-behavior-api` + 1个Starter ⭐
   - 状态: 完成
   - 成果: 用户行为分析、态度推断

### 🔴 P0 - 当前优先级（质量保障）
1. **扩充单元测试** - 从25%提升到80%+
   - 影响: 高
   - 当前: 46个测试用例
   - 目标: 覆盖所有核心模块

2. **更新主README.md** - 完善文档
   - 影响: 高
   - 状态: ✅ 已完成

### 🟡 P1 - 近期优化（性能提升）
1. **知识库加载器** - 性能优化关键
   - LRU缓存机制
   - 预加载策略
   - 加载统计
   
2. **集成测试** - 跨模块测试
   - P2P端到端测试
   - Behavior集成测试
   - Voting集成测试

3. **性能基准测试** - 各存储后端性能对比
   - 数据传输速度测试
   - 内存占用分析

### 🟢 P2 - 长期规划（功能增强）
1. **Behavior Starter扩展**
   - Redis实现
   - MongoDB实现
   - Elasticsearch实现

2. **可视化仪表板**
   - 行为分析可视化
   - P2P连接监控
   - 投票统计展示

3. **文档完善**
   - 快速开始教程
   - 最佳实践指南
   - 故障排查文档

---

---

## 七维可插拔架构的核心优势

### 🎯 对开发者
```
✅ 简单易用
   只需在 pom.xml 中选择依赖，无需编写工厂代码

✅ 类型安全
   编译时检查，IDE 自动提示，减少运行时错误

✅ 降低学习成本
   遵循 Spring Boot 标准，熟悉的开发模式

✅ 快速上手
   默认配置开箱即用，5分钟启动项目
```

### 🏗️ 对架构
```
✅ 高内聚低耦合
   Core 只依赖接口，每个 Starter 独立完整

✅ 开闭原则
   新增实现无需修改现有代码

✅ 单一职责
   每个 Starter 只负责一种实现

✅ 易于测试
   Mock 接口即可，单元测试简单
```

### 🚀 对运维
```
✅ 灵活部署
   开发/测试/生产环境使用不同组合

✅ 平滑迁移
   从本地文件到云存储，无缝切换

✅ 降低成本
   开发用免费方案，生产用企业方案

✅ 故障隔离
   某个维度故障不影响其他维度
```

### 💡 对业务
```
✅ 适应场景多样
   从个人项目到企业应用，都能满足

✅ 技术债务低
   架构清晰，易于维护和演进

✅ 扩展性强
   新需求通过新 Starter 实现

✅ 未来可期
   支持未来的新技术栈（如新的向量数据库）
```

---

## 数据流向图

```
┌─────────────┐
│  用户请求   │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────┐
│          omni-agent-core                 │
│       (核心业务逻辑层)                    │
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ HOPE知识   │───▶│ 持久化接口   │────┼───▶ Persistence Starter
│  └────────────┘    └──────────────┘    │      (Memory/H2/ES...)
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ 文档分块   │───▶│ 存储接口     │────┼───▶ Storage Starter
│  └────────────┘    └──────────────┘    │      (File/MongoDB/S3...)
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ 文档检索   │───▶│ RAG接口      │────┼───▶ RAG Starter
│  └────────────┘    └──────────────┘    │      (Lucene/ES...)
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ AI推理     │───▶│ AI接口       │────┼───▶ AI Starter
│  └────────────┘    └──────────────┘    │      (Ollama/API...)
│                                          │
└──────────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  用户响应   │
└─────────────┘
```

---

## 关键设计决策

### ✅ 为什么选择编译时而非运行时？

**运行时切换（旧方案）**:
```java
// ❌ 需要工厂、策略模式，复杂
PersistenceManager manager = new PersistenceManager();
manager.switchStrategy(PersistenceStrategy.ELASTICSEARCH);
```

**编译时选择（新方案）**:
```xml
<!-- ✅ 简单、清晰、Spring Boot 标准 -->
<dependency>
    <artifactId>omni-agent-persistence-starter-elasticsearch</artifactId>
</dependency>
```

### ✅ 为什么分七个维度？

```
1️⃣ 持久化层 - 存储结构化的元数据、配置
   • 特点: 小数据量、频繁读写、需要事务

2️⃣ 文档存储层 - 存储分块、图像、大文件
   • 特点: 大数据量、少写多读、需要高吞吐

3️⃣ RAG层 - 文档索引、向量检索
   • 特点: 需要全文搜索、向量相似度、排序

4️⃣ AI层 - LLM推理、Embedding生成
   • 特点: 计算密集、可能需要GPU、延迟敏感

5️⃣ P2P协作层 - 端点发现、安全握手、数据传输 ⭐ NEW
   • 特点: 分布式通信、安全连接、跨节点数据流

6️⃣ 投票仲裁层 - 多方投票、冲突解决、民主决策 ⭐ NEW
   • 特点: 多角色参与、加权计算、共识达成

7️⃣ 行为分析层 - 用户信号、态度推断、热度计算 ⭐ NEW
   • 特点: 实时收集、智能推断、多维聚合

职责分离，技术选型灵活，功能完整！
```

### ✅ 为什么不合并维度？

**如果合并持久化和文档存储**:
```
❌ 小配置数据和大图像文件混在一起
❌ ES 擅长搜索但不适合存储大图像
❌ S3 适合大文件但不适合频繁的小事务
❌ 无法针对不同数据特点优化

✅ 分离后各选最优方案
```

---

## 对比其他架构

| 特性 | 工厂模式 | 策略模式 | 插件系统 | Spring Boot Starter ✅ |
|------|----------|----------|----------|------------------------|
| 实现复杂度 | 中 | 中 | 高 | 低 |
| 用户使用难度 | 中 | 高 | 高 | 低 |
| 编译时检查 | ❌ | ❌ | ❌ | ✅ |
| IDE 支持 | 一般 | 一般 | 差 | 优秀 |
| 配置提示 | ❌ | ❌ | ❌ | ✅ |
| 社区生态 | - | - | - | Spring 生态 |
| 学习曲线 | 陡 | 陡 | 陡 | 平缓 |

**结论**: Spring Boot Starter 是最佳选择！

---

## 📊 模块实现统计 (2025-12-15)

### 总体统计
```
总模块数: 45个
已实现模块: 42个 (93%)
编译状态: 42/42 SUCCESS (100%)
代码总量: ~22,000+行
```

### 各维度统计

| 维度 | API模块 | Starter数量 | 完成度 | 状态 |
|------|---------|------------|--------|------|
| 持久化 | omni-agent-persistence-api | 6个 | 100% | ✅ |
| 文档存储 | omni-agent-document-storage-api | 6个 | 100% | ✅ |
| RAG检索 | omni-agent-rag-api | 6个 | 100% | ✅ |
| AI推理 | omni-agent-ai-api | 2个 | 100% | ✅ |
| P2P协作 | omni-agent-p2p-api | 6个 | 100% | ✅ ⭐ |
| 投票仲裁 | omni-agent-voting-api | 4个 | 100% | ✅ ⭐ |
| 行为分析 | omni-agent-behavior-api | 1个 | 100% | ✅ ⭐ |
| **总计** | **7个API** | **31个Starter** | **100%** | **✅** |

### 已实现模块列表

#### API层 (7个模块)
1. ✅ omni-agent-persistence-api
2. ✅ omni-agent-document-storage-api
3. ✅ omni-agent-rag-api
4. ✅ omni-agent-ai-api
5. ✅ omni-agent-p2p-api ⭐
6. ✅ omni-agent-voting-api ⭐
7. ✅ omni-agent-behavior-api ⭐

#### Core层 (1个模块)
8. ✅ omni-agent-core

#### Starter实现层 (31个模块)

**持久化 Starters (6个)**
9. ✅ omni-agent-persistence-starter-memory
10. ✅ omni-agent-persistence-starter-h2
11. ✅ omni-agent-persistence-starter-sqlite
12. ✅ omni-agent-persistence-starter-redis
13. ✅ omni-agent-persistence-starter-mongodb
14. ✅ omni-agent-persistence-starter-elasticsearch

**文档存储 Starters (6个)**
15. ✅ omni-agent-document-storage-starter-file
16. ✅ omni-agent-document-storage-starter-mongodb
17. ✅ omni-agent-document-storage-starter-redis
18. ✅ omni-agent-document-storage-starter-elasticsearch
19. ✅ omni-agent-document-storage-starter-s3
20. ✅ omni-agent-document-storage-starter-minio

**RAG Starters (6个)**
21. ✅ omni-agent-rag-starter-file
22. ✅ omni-agent-rag-starter-h2
23. ✅ omni-agent-rag-starter-sqlite
24. ✅ omni-agent-rag-starter-redis
25. ✅ omni-agent-rag-starter-mongodb
26. ✅ omni-agent-rag-starter-elasticsearch

**AI Starters (2个)**
27. ✅ omni-agent-ai-starter-ollama
28. ✅ omni-agent-ai-starter-online-api

**P2P Starters (6个)** ⭐
29. ✅ omni-agent-p2p-starter-memory
30. ✅ omni-agent-p2p-starter-h2
31. ✅ omni-agent-p2p-starter-sqlite
32. ✅ omni-agent-p2p-starter-redis
33. ✅ omni-agent-p2p-starter-mongodb
34. ✅ omni-agent-p2p-starter-elasticsearch

**Voting Starters (4个)** ⭐
35. ✅ omni-agent-voting-starter-memory
36. ✅ omni-agent-voting-starter-redis
37. ✅ omni-agent-voting-starter-mongodb
38. ✅ omni-agent-voting-starter-elasticsearch

**Behavior Starters (1个)** ⭐
39. ✅ omni-agent-behavior-starter-memory

#### 示例应用 (2个模块)
40. ✅ omni-agent-example-basic
41. ✅ omni-agent-example-production

#### UI层 (1个模块)
42. ✅ UI (前端界面)

### 架构成就 ⭐ **P1进展**
- 🎉 **七维架构100%完成**
- 🎉 **31个Starter全部实现**
- 🎉 **编译100%通过**
- 🎉 **10,368种组合可能**
- 🎉 **框架成熟度95%**
- 🎉 **P1-1完成**: 知识库加载器（LRU缓存+预加载+统计）✅
- 🎯 **测试覆盖率**: 20%（56个测试用例）

---

**文档版本**: v3.0 (七维可插拔架构)  
**创建时间**: 2025-12-14  
**更新时间**: 2025-12-15  
**适用场景**: 代码重构规划、模块依赖分析、架构优化、可插拔设计  
**维护者**: OmniAgent Team

---

> 🎯 **架构目标**: 打造完全可插拔的全场景 Agent 框架！  
> 🔌 **核心理念**: 7个维度，10,368种组合，业务代码零改动！  
> 🚀 **使用体验**: 像搭积木一样组装系统！

