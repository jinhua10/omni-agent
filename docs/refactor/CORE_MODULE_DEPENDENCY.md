# OmniAgent Core 模块依赖关系图

## 模块依赖结构（四维可插拔架构）

> **架构理念**: 基于 Spring Boot Starter 模式的四维可插拔架构  
> **设计原则**: 依赖倒置 • 接口隔离 • 编译时选择 • 运行时注入

```
                         ┌─────────────────────────────────────────┐
                         │        用户应用 (Application)           │
                         │    通过依赖选择组合不同的实现           │
                         └────────────────┬────────────────────────┘
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    │                     │                     │
            ┌───────▼───────┐     ┌──────▼──────┐     ┌───────▼───────┐
            │  选择持久化    │     │  选择存储    │     │  选择检索    │
            │  Starter      │     │  Starter    │     │  Starter    │
            └───────┬───────┘     └──────┬──────┘     └───────┬───────┘
                    │                     │                     │
                    └─────────────────────┼─────────────────────┘
                                          │
                         ┌────────────────▼────────────────┐
                         │     omni-agent-core             │
                         │     (核心业务逻辑层)             │
                         │   只依赖接口，不依赖实现         │
                         └────────────────┬────────────────┘
                                          │
        ┌─────────────────────────────────┼─────────────────────────────────┐
        │                                 │                                 │
┌───────▼────────┐              ┌────────▼────────┐              ┌────────▼────────┐
│  核心业务域    │              │  文档处理域      │              │  交互增强域      │
│                │              │                 │              │                 │
│ • HOPE 知识    │              │ • 文档分块      │              │ • 查询处理      │
│ • 角色系统     │              │ • 图像提取      │              │ • 反馈系统      │
│ • 概念演化     │              │ • PPL 引擎      │              │ • 投票系统      │
└────────┬───────┘              └────────┬────────┘              └────────┬────────┘
         │                                │                                │
         │                                │                                │
         └────────────────────────────────┼────────────────────────────────┘
                                          │
                         ┌────────────────▼────────────────┐
                         │      API 接口层 (抽象)          │
                         │   定义标准规范，不含实现         │
                         └────────────────┬────────────────┘
                                          │
        ┌─────────────────────────────────┼─────────────────────────────────┐
        │                                 │                                 │
┌───────▼───────────┐          ┌─────────▼─────────┐          ┌───────────▼───────┐
│  Persistence API  │          │ Document Storage  │          │     RAG API       │
│  (持久化接口)      │          │      API          │          │   (检索接口)      │
│                   │          │  (文档存储接口)    │          │                   │
│  接口:            │          │                   │          │  接口:            │
│  • Persistence    │          │  接口:            │          │  • RAGService     │
│  • Config         │          │  • Storage        │          │  • Indexer        │
│                   │          │  • ChunkStorage   │          │  • VectorSearch   │
│  模型:            │          │  • ImageStorage   │          │                   │
│  • TypeConfig     │          │                   │          │  模型:            │
│                   │          │  模型:            │          │  • Document       │
│                   │          │  • Chunk          │          │  • SearchResult   │
│                   │          │  • Image          │          │                   │
└───────┬───────────┘          └─────────┬─────────┘          └───────────┬───────┘
        │                                 │                                 │
        │                                 │                                 │
        │  🔌 可插拔维度 1                 │  🔌 可插拔维度 2                 │  🔌 可插拔维度 3
        │  (结构化数据存储)                │  (文档/大文件存储)              │  (文档索引检索)
        │                                 │                                 │
┌───────▼───────────┐          ┌─────────▼─────────┐          ┌───────────▼───────┐
│ 持久化 Starters   │          │ 存储 Starters     │          │  RAG Starters     │
│ (选择其一)        │          │ (选择其一)        │          │  (选择其一)       │
│                   │          │                   │          │                   │
│ ☐ memory          │          │ ☐ file (本地)     │          │ ☐ file (Lucene)   │
│ ☐ h2              │          │ ☐ mongodb (GridFS)│          │ ☐ h2              │
│ ☐ sqlite          │          │ ☐ s3 (AWS S3)     │          │ ☐ sqlite          │
│ ☐ redis           │          │ ☐ minio (私有云)  │          │ ☐ redis           │
│ ☐ mongodb         │          │ ☐ redis           │          │ ☐ mongodb         │
│ ☐ elasticsearch   │          │ ☐ elasticsearch   │          │ ☐ elasticsearch   │
│                   │          │                   │          │                   │
│ 每个 Starter 包含:│          │ 每个 Starter 包含:│          │ 每个 Starter 包含:│
│ • 完整实现        │          │ • 完整实现        │          │ • 完整实现        │
│ • 自动配置        │          │ • 自动配置        │          │ • 自动配置        │
│ • 配置属性        │          │ • 配置属性        │          │ • 配置属性        │
└───────────────────┘          └───────────────────┘          └───────────────────┘

                                          │
                         ┌────────────────▼────────────────┐
                         │        AI 引擎 API              │
                         │      (LLM 推理接口)             │
                         └────────────────┬────────────────┘
                                          │
                                          │  🔌 可插拔维度 4
                                          │  (AI 推理引擎)
                                          │
                         ┌────────────────▼────────────────┐
                         │      AI Starters (选择其一)     │
                         │                                 │
                         │  ☐ local-ollama (本地)          │
                         │  ☐ remote-ollama (远程)         │
                         │  ☐ online-api (在线API)         │
                         │                                 │
                         │  每个 Starter 包含:             │
                         │  • 完整实现                     │
                         │  • 自动配置                     │
                         │  • 配置属性                     │
                         └─────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

                         架构关键特性说明

  🎯 依赖倒置原则 (DIP)
     Core → 依赖 → 接口 API
     Starter → 实现 → 接口 API
     高层不依赖低层，都依赖抽象

  🔌 编译时选择
     在 pom.xml 中选择需要的 Starter
     <dependency>
       <artifactId>omni-agent-persistence-starter-elasticsearch</artifactId>
     </dependency>

  🚀 运行时自动配置
     Spring Boot 根据类路径自动注入实现
     @Autowired
     private QuestionClassifierPersistence persistence; // 自动注入

  🎨 灵活组合
     4 个维度独立选择，任意组合：
     • 持久化: 6 种选择
     • 文档存储: 6 种选择
     • RAG: 6 种选择
     • AI: 3 种选择
     = 6 × 6 × 6 × 3 = 648 种组合！

  💡 切换无需改代码
     只需修改 pom.xml 依赖和 application.yml 配置
     业务代码保持不变

═══════════════════════════════════════════════════════════════════════════
```

---

## 四维可插拔架构使用示例

### 场景 1: 个人开发者（快速启动）

**pom.xml 依赖配置**:
```xml
<dependencies>
    <!-- 维度1: 持久化 - 内存（无需外部服务） -->
    <dependency>
        <groupId>top.yumbo.ai</groupId>
        <artifactId>omni-agent-persistence-starter-memory</artifactId>
    </dependency>
    
    <!-- 维度2: 文档存储 - 本地文件（简单快速） -->
    <dependency>
        <groupId>top.yumbo.ai</groupId>
        <artifactId>omni-agent-document-storage-starter-file</artifactId>
    </dependency>
    
    <!-- 维度3: RAG - Lucene本地（无需外部服务） -->
    <dependency>
        <groupId>top.yumbo.ai</groupId>
        <artifactId>omni-agent-rag-starter-file</artifactId>
    </dependency>
    
    <!-- 维度4: AI - 本地Ollama（离线可用） -->
    <dependency>
        <groupId>top.yumbo.ai</groupId>
        <artifactId>omni-agent-ai-starter-local-ollama</artifactId>
    </dependency>
</dependencies>
```

**application.yml 配置**:
```yaml
omni-agent:
  persistence:
    type: memory
  document-storage:
    type: file
    file:
      base-path: ./data/storage
  rag:
    type: file
    file:
      index-path: ./data/lucene
  ai:
    type: local-ollama
    local-ollama:
      base-url: http://localhost:11434
```

**特点**: ✅ 零外部依赖 ✅ 快速启动 ✅ 适合开发调试

---

### 场景 2: 小型团队（混合部署）

**pom.xml 依赖配置**:
```xml
<dependencies>
    <!-- 维度1: 持久化 - H2数据库（嵌入式） -->
    <dependency>
        <artifactId>omni-agent-persistence-starter-h2</artifactId>
    </dependency>
    
    <!-- 维度2: 文档存储 - MongoDB（适合文档） -->
    <dependency>
        <artifactId>omni-agent-document-storage-starter-mongodb</artifactId>
    </dependency>
    
    <!-- 维度3: RAG - Elasticsearch（生产级） -->
    <dependency>
        <artifactId>omni-agent-rag-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度4: AI - 远程Ollama（团队共享） -->
    <dependency>
        <artifactId>omni-agent-ai-starter-remote-ollama</artifactId>
    </dependency>
</dependencies>
```

**特点**: ✅ 结构化数据用H2 ✅ 文档/图像用MongoDB ✅ 检索用ES ✅ AI共享使用

---

### 场景 3: 企业生产（云原生）

**pom.xml 依赖配置**:
```xml
<dependencies>
    <!-- 维度1: 持久化 - Elasticsearch集群 -->
    <dependency>
        <artifactId>omni-agent-persistence-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度2: 文档存储 - AWS S3（高可靠） -->
    <dependency>
        <artifactId>omni-agent-document-storage-starter-s3</artifactId>
    </dependency>
    
    <!-- 维度3: RAG - Elasticsearch（同一集群） -->
    <dependency>
        <artifactId>omni-agent-rag-starter-elasticsearch</artifactId>
    </dependency>
    
    <!-- 维度4: AI - 在线API（企业级） -->
    <dependency>
        <artifactId>omni-agent-ai-starter-online-api</artifactId>
    </dependency>
</dependencies>
```

**特点**: ✅ 高可用 ✅ 可扩展 ✅ 云托管 ✅ 企业级

---

### 切换场景：从开发到生产

**业务代码（无需修改）**:
```java
@Service
public class DocumentService {
    
    // 注入接口，不关心具体实现
    @Autowired
    private QuestionClassifierPersistence persistence;
    
    @Autowired
    private DocumentStorageService storageService;
    
    @Autowired
    private RAGService ragService;
    
    @Autowired
    private AIService aiService;
    
    public void processDocument(Document doc) {
        // 业务逻辑保持不变
        // Spring Boot 会根据选择的 Starter 自动注入对应实现
        
        // 存储元数据（可能是 Memory、H2、ES...）
        persistence.saveQuestionType(config);
        
        // 存储分块（可能是 File、MongoDB、S3...）
        storageService.saveChunk(doc.getId(), chunk);
        
        // 索引文档（可能是 Lucene、ES...）
        ragService.indexDocument(doc);
        
        // AI 处理（可能是 Ollama、OpenAI...）
        aiService.process(request);
    }
}
```

**只需修改**:
1. `pom.xml` - 更换依赖
2. `application.yml` - 更新配置
3. 业务代码 - 无需修改！✅

---

## 模块调用链路

### 1. 查询处理链路
```
用户查询
  │
  ├─> QueryProcessor (查询处理器)
  │     │
  │     ├─> HOPEKnowledgeManager (HOPE 知识管理)
  │     │     │
  │     │     ├─> HighFrequencyLayerService (高频层)
  │     │     ├─> OrdinaryLayerService (普通层)
  │     │     └─> PermanentLayerService (永久层)
  │     │
  │     ├─> DocumentChunker (文档分块)
  │     │     │
  │     │     └─> ChunkingStrategy (分块策略)
  │     │           ├─> SimpleChunkingStrategy
  │     │           ├─> PPLChunkingStrategy
  │     │           └─> LLMChunkingStrategy
  │     │
  │     └─> RoleDetector (角色检测)
  │           │
  │           └─> AIRoleAnalyzer (AI 角色分析)
  │
  └─> 返回 SearchResult
```

### 2. 文档索引链路
```
文档上传
  │
  ├─> DocumentUtils (文档工具)
  │     │
  │     ├─> DocumentImageExtractor (图像提取)
  │     │     ├─> PDFImageExtractor
  │     │     ├─> WordImageExtractor
  │     │     ├─> ExcelImageExtractor
  │     │     └─> PowerPointImageExtractor
  │     │
  │     └─> DocumentChunker (文档分块)
  │           │
  │           ├─> ChunkStorageService (分块存储)
  │           └─> PPLService (PPL 处理)
  │
  ├─> ConceptExtractor (概念提取)
  │     │
  │     └─> HOPEConceptExtractor (HOPE 概念提取)
  │
  └─> 存储到索引
```

### 3. 知识演化链路
```
知识冲突检测
  │
  ├─> ConflictDetector (冲突检测器)
  │     │
  │     └─> SimilarityCalculator (相似度计算)
  │
  ├─> VotingService (投票服务)
  │     │
  │     ├─> VotingSession (投票会话)
  │     └─> VotingArbiter (投票仲裁)
  │
  └─> ConceptEvolutionService (概念演化)
        │
        └─> ConceptUpdater (概念更新)
              │
              └─> VersionManager (版本管理)
```

### 4. 反馈处理链路
```
用户反馈
  │
  ├─> FeedbackCollector (反馈收集)
  │     │
  │     ├─> BehaviorAnalyzer (行为分析)
  │     │     │
  │     │     └─> SignalAggregator (信号聚合)
  │     │
  │     └─> AttitudeInferenceEngine (态度推理)
  │
  ├─> DocumentWeightService (文档权重)
  │
  └─> QARecordService (QA 记录)
```

---

## 模块间依赖强度

### 强依赖（紧耦合）
- `hope` ← → `persistence` (持久化是 HOPE 的核心依赖)
- `chunking` ← → `ppl` (分块严重依赖 PPL)
- `role` ← → `concept` (角色系统依赖概念管理)

### 中依赖（适度耦合）
- `query` ← → `hope` (查询使用 HOPE 增强)
- `evolution` ← → `voting` (演化需要投票机制)
- `service` ← → `ai` (业务服务调用 AI 引擎)

### 弱依赖（松耦合）
- `feedback` ← → `gamification` (反馈触发游戏化)
- `archive` ← → `query` (归档存储查询历史)
- `monitor` ← → `quality` (监控关注质量指标)

---

## 建议的重构模块划分

### 方案 1: 按业务域划分
```
omni-agent-knowledge (知识管理域)
  ├─ hope (HOPE 系统)
  ├─ concept (概念管理)
  ├─ evolution (演化系统)
  └─ role (角色系统)

omni-agent-document (文档处理域)
  ├─ chunking (文档分块)
  ├─ image (图像处理)
  └─ ppl (PPL 引擎)

omni-agent-interaction (交互域)
  ├─ query (查询系统)
  ├─ feedback (反馈系统)
  ├─ voting (投票系统)
  └─ service (业务服务)

omni-agent-infrastructure (基础设施域)
  ├─ persistence (持久化) → 已独立
  ├─ config (配置管理)
  ├─ ai (AI 引擎)
  └─ util (工具类)
```

### 方案 2: 按技术层次划分
```
omni-agent-domain (领域层)
  ├─ model (领域模型)
  ├─ repository (仓库接口)
  └─ service (领域服务)

omni-agent-application (应用层)
  ├─ query (查询应用)
  ├─ command (命令应用)
  └─ integration (集成服务)

omni-agent-infrastructure (基础设施层)
  ├─ persistence (持久化实现)
  ├─ ai (AI 集成)
  └─ monitoring (监控)

omni-agent-common (公共层)
  ├─ util (工具类)
  ├─ config (配置)
  └─ i18n (国际化)
```

---

## 重构优先级建议

### 🔴 P0 - 立即重构（架构性问题）
1. **持久化模块分离** - `hope/persistence` → `omni-agent-persistence-api`
   - 影响: 高
   - 复杂度: 中
   - 收益: 解耦核心模块

2. **RAG 接口标准化** - 定义统一的 RAG API
   - 影响: 高
   - 复杂度: 高
   - 收益: 模块可插拔

### 🟡 P1 - 近期重构（结构性改进）
1. **文档分块模块整合**
   - 清理 `optimization/DocumentChunker` 
   - 统一到 `chunking/*`
   
2. **配置管理统一**
   - 整合各模块配置
   - 使用 Spring Boot Configuration Properties

3. **角色系统优化**
   - 简化角色检测逻辑
   - 提升知识聚合性能

### 🟢 P2 - 长期优化（功能性增强）
1. **HOPE 系统文档化**
2. **监控指标完善**
3. **游戏化功能增强**

---

---

## 四维可插拔架构的核心优势

### 🎯 对开发者
```
✅ 简单易用
   只需在 pom.xml 中选择依赖，无需编写工厂代码

✅ 类型安全
   编译时检查，IDE 自动提示，减少运行时错误

✅ 降低学习成本
   遵循 Spring Boot 标准，熟悉的开发模式

✅ 快速上手
   默认配置开箱即用，5分钟启动项目
```

### 🏗️ 对架构
```
✅ 高内聚低耦合
   Core 只依赖接口，每个 Starter 独立完整

✅ 开闭原则
   新增实现无需修改现有代码

✅ 单一职责
   每个 Starter 只负责一种实现

✅ 易于测试
   Mock 接口即可，单元测试简单
```

### 🚀 对运维
```
✅ 灵活部署
   开发/测试/生产环境使用不同组合

✅ 平滑迁移
   从本地文件到云存储，无缝切换

✅ 降低成本
   开发用免费方案，生产用企业方案

✅ 故障隔离
   某个维度故障不影响其他维度
```

### 💡 对业务
```
✅ 适应场景多样
   从个人项目到企业应用，都能满足

✅ 技术债务低
   架构清晰，易于维护和演进

✅ 扩展性强
   新需求通过新 Starter 实现

✅ 未来可期
   支持未来的新技术栈（如新的向量数据库）
```

---

## 数据流向图

```
┌─────────────┐
│  用户请求   │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────┐
│          omni-agent-core                 │
│       (核心业务逻辑层)                    │
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ HOPE知识   │───▶│ 持久化接口   │────┼───▶ Persistence Starter
│  └────────────┘    └──────────────┘    │      (Memory/H2/ES...)
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ 文档分块   │───▶│ 存储接口     │────┼───▶ Storage Starter
│  └────────────┘    └──────────────┘    │      (File/MongoDB/S3...)
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ 文档检索   │───▶│ RAG接口      │────┼───▶ RAG Starter
│  └────────────┘    └──────────────┘    │      (Lucene/ES...)
│                                          │
│  ┌────────────┐    ┌──────────────┐    │
│  │ AI推理     │───▶│ AI接口       │────┼───▶ AI Starter
│  └────────────┘    └──────────────┘    │      (Ollama/API...)
│                                          │
└──────────────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  用户响应   │
└─────────────┘
```

---

## 关键设计决策

### ✅ 为什么选择编译时而非运行时？

**运行时切换（旧方案）**:
```java
// ❌ 需要工厂、策略模式，复杂
PersistenceManager manager = new PersistenceManager();
manager.switchStrategy(PersistenceStrategy.ELASTICSEARCH);
```

**编译时选择（新方案）**:
```xml
<!-- ✅ 简单、清晰、Spring Boot 标准 -->
<dependency>
    <artifactId>omni-agent-persistence-starter-elasticsearch</artifactId>
</dependency>
```

### ✅ 为什么分四个维度？

```
1️⃣ 持久化层 - 存储结构化的元数据、配置
   • 特点: 小数据量、频繁读写、需要事务

2️⃣ 文档存储层 - 存储分块、图像、大文件
   • 特点: 大数据量、少写多读、需要高吞吐

3️⃣ RAG层 - 文档索引、向量检索
   • 特点: 需要全文搜索、向量相似度、排序

4️⃣ AI层 - LLM推理、Embedding生成
   • 特点: 计算密集、可能需要GPU、延迟敏感

职责分离，技术选型灵活！
```

### ✅ 为什么不合并维度？

**如果合并持久化和文档存储**:
```
❌ 小配置数据和大图像文件混在一起
❌ ES 擅长搜索但不适合存储大图像
❌ S3 适合大文件但不适合频繁的小事务
❌ 无法针对不同数据特点优化

✅ 分离后各选最优方案
```

---

## 对比其他架构

| 特性 | 工厂模式 | 策略模式 | 插件系统 | Spring Boot Starter ✅ |
|------|----------|----------|----------|------------------------|
| 实现复杂度 | 中 | 中 | 高 | 低 |
| 用户使用难度 | 中 | 高 | 高 | 低 |
| 编译时检查 | ❌ | ❌ | ❌ | ✅ |
| IDE 支持 | 一般 | 一般 | 差 | 优秀 |
| 配置提示 | ❌ | ❌ | ❌ | ✅ |
| 社区生态 | - | - | - | Spring 生态 |
| 学习曲线 | 陡 | 陡 | 陡 | 平缓 |

**结论**: Spring Boot Starter 是最佳选择！

---

**文档版本**: v2.0 (四维可插拔架构)  
**创建时间**: 2025-12-14  
**更新时间**: 2025-12-14  
**适用场景**: 代码重构规划、模块依赖分析、架构优化、可插拔设计

---

> 🎯 **架构目标**: 打造完全可插拔的全场景 Agent 框架！  
> 🔌 **核心理念**: 4个维度，648种组合，业务代码零改动！  
> 🚀 **使用体验**: 像搭积木一样组装系统！

