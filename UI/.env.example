# 前端环境变量配置示例
# Frontend Environment Variables Example

# ============================================================================
# API 配置 / API Configuration
# ============================================================================

# API 基础路径（可选，默认使用 Vite 代理）
# API Base URL (Optional, defaults to Vite proxy)
# 开发环境：使用相对路径 /api，由 vite.config.js 代理到 http://localhost:8080
# Development: Use relative path /api, proxied by vite.config.js to http://localhost:8080
# 生产环境：可以设置为完整的后端地址
# Production: Can be set to full backend URL
# VITE_API_BASE_URL=/api

# React App API Base URL (兼容旧配置)
# REACT_APP_API_BASE_URL=/api

# ============================================================================
# 应用配置 / Application Configuration
# ============================================================================

# 应用标题
# VITE_APP_TITLE=AI智能审查系统

# 应用描述
# VITE_APP_DESCRIPTION=AI-powered intelligent review system

# ============================================================================
# 功能开关 / Feature Flags
# ============================================================================

# 是否启用 Mock 数据（开发时使用）
# VITE_ENABLE_MOCK=false

# 是否启用调试模式
# VITE_DEBUG_MODE=true

# ============================================================================
# 注意事项 / Notes
# ============================================================================
# 1. 复制此文件为 .env.local 或 .env.development 进行自定义配置
# 2. .env.local 文件不会被 git 跟踪
# 3. Vite 环境变量必须以 VITE_ 开头才能在客户端访问
# 4. 修改环境变量后需要重启开发服务器
