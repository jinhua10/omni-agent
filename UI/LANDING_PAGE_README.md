# OmniAgent Landing Page 说明文档

## 📋 概述

已为OmniAgent创建了一个类似CodeGeex的炫酷官方首页。用户首次访问时会看到Landing Page，点击"立即开始"按钮后才会进入主应用。

## 🎨 设计特点

### 1. Hero Section（英雄区）
- **渐变背景**：紫色渐变背景（#667eea → #764ba2）
- **浮动粒子动画**：背景有动态浮动效果
- **Logo展示**：API图标 + OmniAgent标题
- **CTA按钮**：
  - 立即开始（进入应用）
  - GitHub链接
  - 技术博客链接（CSDN）
- **动态统计数据**：
  - 20+ Maven模块
  - 15,000+ 代码行数
  - 5种文档格式
  - 6种分块策略

### 2. 核心理念对比区
- **左侧**：传统RAG的问题（红色调）
  - 单一索引池
  - 向量空间混乱
  - 检索精度低下
- **中间**：VS对比标志
- **右侧**：OmniAgent解决方案（绿色调）
  - 知识域隔离
  - 独立向量空间
  - 专业化处理

### 3. 工作原理流程图
展示完整的处理流程：
```
文档上传 → 智能提取 → 智能分块 → 向量化 → 域索引 → 语义检索
```
每个步骤都有图标和说明。

### 4. 核心特性卡片
6个特性卡片，每个包含：
- 图标
- 标题
- 描述
- 功能列表（带✓图标）

特性包括：
- 知识域隔离
- 智能文档处理
- 6种分块策略
- 向量化检索
- 知识网络
- 企业级架构

### 5. 技术栈展示
使用shields.io徽章展示技术栈：
- Java 21
- Spring Boot 3.4.1
- Apache Lucene 9.10
- ONNX Runtime
- React 18
- Ant Design 5

### 6. 三步启动指南
3个卡片展示快速开始步骤：
1. 克隆项目
2. 编译项目
3. 运行示例

每个卡片都有步骤编号和命令代码块。

### 7. Footer
包含三列信息：
- 关于OmniAgent
- 快速链接（GitHub、Gitee、技术博客）
- 联系方式（邮箱、GitHub）

## 📁 文件结构

```
UI/src/components/landing/
├── LandingPage.jsx       # 主组件
├── LandingPage.css       # 样式文件
└── index.js              # 导出文件
```

## 🔧 技术实现

### 路由逻辑

在`App.jsx`中添加了Landing Page显示逻辑：

```javascript
const [showLanding, setShowLanding] = useState(() => {
  // 检查是否首次访问
  const hasVisited = localStorage.getItem('omni_has_visited')
  const hasHash = window.location.hash && window.location.hash.length > 1
  return !hasVisited && !hasHash
})

const handleEnterApp = () => {
  setShowLanding(false)
  localStorage.setItem('omni_has_visited', 'true')
  window.location.hash = '#/qa'
}
```

### 动画效果

1. **数字动画**：统计数据从0到目标值的动画
2. **浮动动画**：背景粒子的上下浮动
3. **脉冲动画**：Logo图标的呼吸效果
4. **渐入动画**：特性卡片的淡入向上效果
5. **悬停效果**：
   - 按钮悬停上浮
   - 卡片悬停上浮+阴影
   - 图标悬停旋转放大

### 响应式设计

- 大屏（>768px）：多列布局
- 小屏（≤768px）：单列布局
- 工作流图：大屏横向，小屏竖向
- 按钮：小屏100%宽度

## 🎯 使用方法

### 1. 首次访问
用户首次访问应用时会自动显示Landing Page。

### 2. 进入应用
点击任何"立即开始"按钮后：
- 隐藏Landing Page
- 设置localStorage标记
- 跳转到主应用（QA页面）

### 3. 重新显示Landing Page
如果想再次显示Landing Page，可以：
- 清除浏览器localStorage中的`omni_has_visited`键
- 或直接访问根URL（不带hash）

## 🎨 自定义配置

### 修改颜色主题

在`LandingPage.css`中修改：

```css
/* Hero背景渐变 */
.hero-section {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

/* 主色调 */
.btn-primary {
  color: #667eea !important;
}
```

### 修改统计数据

在`LandingPage.jsx`中修改：

```javascript
const targets = {
  modules: 20,        // 模块数
  codeLines: 15000,   // 代码行数
  formats: 5,         // 文档格式数
  strategies: 6       // 分块策略数
}
```

### 修改链接地址

在`LandingPage.jsx`中修改：

```javascript
// GitHub
href="https://github.com/jinhua10/omni-agent"

// Gitee
href="https://gitee.com/gnnu"

// CSDN博客
href="https://yumbo.blog.csdn.net/"

// 邮箱
1015770492@qq.com
```

## 🚀 启动方式

```bash
# 进入UI目录
cd UI

# 安装依赖（首次）
npm install

# 启动开发服务器
npm run dev

# 访问
http://localhost:5173
```

## 📱 截图预览

Landing Page包含以下区域：
1. **Hero Section** - 大标题+按钮+统计数据
2. **对比区** - 传统RAG vs OmniAgent
3. **流程图** - 6步处理流程
4. **特性卡片** - 6个核心特性
5. **技术栈** - 6个技术徽章
6. **快速开始** - 3步启动指南
7. **Footer** - 链接和联系方式

## 🎯 核心功能

✅ 首次访问自动显示Landing Page  
✅ 点击按钮进入主应用  
✅ 记住用户访问状态  
✅ 炫酷的动画效果  
✅ 完全响应式设计  
✅ 符合CodeGeex风格  
✅ 展示核心工作原理  
✅ 包含所有重要链接  

## 🔗 相关链接

- **GitHub**: https://github.com/jinhua10/omni-agent
- **Gitee**: https://gitee.com/gnnu
- **CSDN博客**: https://yumbo.blog.csdn.net/
- **邮箱**: 1015770492@qq.com

---

**创建日期**: 2025-12-29  
**作者**: GitHub Copilot  
**审核**: Jinhua Yu

