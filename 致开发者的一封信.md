# 致开发者的一封信

亲爱的开发者朋友们：

感谢你关注 omni-agent 项目。在你决定是否使用这个框架之前，我想和你坦诚地聊聊我做这个项目的初衷，以及它与当前主流方案的本质区别。这将帮助你判断这个框架是否适合你的场景。

## 为什么要做 omni-agent？

现如今的 RAG（检索增强生成）存在着严重的设计缺陷。

如果你只是想做一个**聊天机器人**，那么目前的 RAG 是够用的。但如果你想让 **AI 帮你干活，作为生产工具**，那么当下普遍的架构就会暴露问题：

**传统 RAG 流程：**
```
数据 → 分块 → 向量化 → 向量数据库存储 → RAG完成
用户提问 → 提炼问题 → 问题转换为语义向量 → 向量数据库检索 
→ 返回对应的块拼接到上下文 → 调用大模型生成回答
```

**可能造成的结果：** 你认为 AI 在帮你干活，但实际上 AI 只是在做文本补全，在一本正经地胡说八道，**不能保证准确性**。

## 问题的本质

让我们理清几个事实：

1. **目前市面上的通用大模型都很强大** —— 相当于孩子 IQ 很高，但是专业知识缺乏，运用到工作时会遇到各种水土不服。

2. **AI 确实能够帮你干活** —— 我们已经看到了 Cursor、Copilot 等工具的出现，并且真实地在工作中发挥了作用。

3. **前提条件是关键** —— 你喂给 AI 的内容**不丢信息**、**内容完整**，并且你**清楚你的需求**、**描述得清楚**，那么 AI 可以基于你给的信息利用它的推理能力帮你干活。

可以把大语言模型比作一堆带有概率的数据。当你把问题喂给 AI 后，它只不过是在一堆数据中检索和你问题最相关的内容，然后基于这些内容进行推理，给你一个答案。

你使用的 AI 模型也只是基于现阶段人类所有遇到的问题和解决方案做的一个**概率统计模型**。目前的 AI 模型各有专长，源于它们训练的模型、训练的方向并不同，因此这只是一个概率问题。

**核心问题：** 没有大数据的支持以及训练，现阶段的 AI 应用生成的答案可靠性是一个大大的问号？

另外还有一个问题：AI 模型的上下文窗口不是无限的。假如说你有个大上下文窗口的 AI 模型，例如有 100 万 Token 的上下文窗口，真正使用的时候，你还是会遇到"答非所问"等问题——**你喂的内容越多，进行检索时离你的答案越偏离**。

## omni-agent 与 Skill-based Agent 的区别

目前关于 Agent 的 Skill 声音很大，包括 Copilot 等工具都在使用 Skill 做 Agent。我们的系统框架也有计划实现 Skill，但**本质区别**在于：

### Skill-based Agent（如 Copilot）
- **适用场景：** 主要针对代码类场景和软件开发人员
- **工作方式：** 动态调用工具，实时获取上下文信息
- **优点：** 灵活、适合结构化的编程任务
- **缺点：**
  - 需要频繁调用 AI，成本较高
  - 每次都是动态获取的上下文信息，不够精确
  - 数据源需要专门有人来整理和管理
  - **不利于个人使用**

### omni-agent 框架
- **适用场景：** **非代码类场景** + 没有软件开发编程基础的人员
- **核心理念：** 提供一个**更精确的上下文**
- **工作方式：** 构建知识网络，基于完整信息进行推理
- **优点：**
  - 如果知识网络构建好了，理论上比 Skill 调用工具获取生成的上下文**更精确**
  - 减少 AI 调用频率，降低成本
  - **每个人都有个 Agent** —— 通过后期提供更多的 AI 服务，每一次使用 AI 的业务信息都会让知识网络更完善
  - **可持续增长的方案** —— 越用越精准，越用越智能

## 我们提供什么？

**我们的目标是：** 让每个人都能轻松使用 Agent 作为生产工具，并且随着使用不断成长。

- **更精确的上下文：** 基于知识网络构建，而不是碎片化的向量块
- **个人化的 Agent：** 推荐每个人都有自己的 Agent，随着使用不断完善
- **可持续增长：** 每次使用都让你的知识网络更完善，形成正向循环
- **低门槛：** 即使没有编程基础，也能作为生产工具使用

## 这个框架适合你吗？

**适合你的情况：**
- ✅ 你需要 AI 作为**生产工具**，而不仅仅是聊天机器人
- ✅ 你的场景是**非代码类**的专业领域
- ✅ 你希望 AI 的回答**准确可靠**，而不是概率性的猜测
- ✅ 你希望有一个**个人化的 Agent**，随着使用不断成长
- ✅ 你愿意构建和维护自己的知识网络

**可能不适合你的情况：**
- ❌ 你只需要一个简单的聊天机器人
- ❌ 你的场景主要是代码开发，Copilot 等工具已经足够
- ❌ 你不想投入时间构建知识网络
- ❌ 你的需求是一次性的，不需要持续积累

## 坦诚地说

**这个方案并非完美**，或许你有更好的方案，我很乐意学习。

我做 omni-agent，就是为了解决"AI 作为生产工具时准确性不足"的问题。如果你也遇到过这个困扰，如果你也相信"完整的信息 + AI 的推理能力"能创造价值，那么欢迎你尝试这个框架。

如果你发现它不适合你的场景，也完全没关系 —— 选择最适合你的工具，才是最重要的。

## 最后

感谢你花时间阅读这封信。

无论你是否选择使用 omni-agent，我都希望这封信能帮助你更清晰地思考：**在你的场景中，什么样的 AI 方案才是最合适的？**

如果你决定尝试，欢迎加入我们。  
如果你有更好的想法，也欢迎交流分享。

祝好，

omni-agent 作者

---

*P.S. 如果你想了解更多技术细节，欢迎查阅项目文档或提 Issue 交流。*

